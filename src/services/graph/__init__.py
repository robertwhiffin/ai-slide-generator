"""LangGraph deck-generation graph package.

This package holds the compiled LangGraph graph and every module that shapes
its state and behaviour.  The state contract lives in ``state.py``; graph
assembly and node definitions are added by later tasks in ws4c.
"""
