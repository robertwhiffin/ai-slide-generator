/**
 * Hook for managing configuration profiles.
 * 
 * This is now a re-export of the ProfileContext for backwards compatibility.
 * Use this hook to access profile state and methods.
 */

export { useProfiles } from '../contexts/ProfileContext';

