# AI Slide Generator - Environment Configuration
# Copy this file to .env and fill in your values

# ============================================================================
# Databricks Configuration (REQUIRED)
# ============================================================================

# Your Databricks workspace URL (must start with https://)
DATABRICKS_HOST=https://your-workspace.cloud.databricks.com

# Your Databricks personal access token
# Create one at: Workspace Settings > Developer > Access Tokens
DATABRICKS_TOKEN=dapi...your-token-here

# ============================================================================
# Database Configuration (REQUIRED)
# ============================================================================

# Option 1: PostgreSQL (Local Development - Recommended)
# Format: postgresql://[user[:password]@][host][:port]/database
DATABASE_URL=postgresql://localhost:5432/ai_slide_generator

# Option 2: Lakebase/Unity Catalog (Production on Databricks)
# Format: databricks://token:${DATABRICKS_TOKEN}@${DATABRICKS_HOST}?http_path=/sql/1.0/warehouses/${WAREHOUSE_ID}&catalog=${CATALOG}&schema=${SCHEMA}
# Example:
# DATABASE_URL=databricks://token:${DATABRICKS_TOKEN}@${DATABRICKS_HOST}?http_path=/sql/1.0/warehouses/abc123def&catalog=main&schema=ai_slide_generator

# Option 3: Remote PostgreSQL
# DATABASE_URL=postgresql://username:password@hostname:5432/ai_slide_generator

# ============================================================================
# Development Environment (Optional - Auto-set by start_app.sh)
# ============================================================================

# Environment mode (development, staging, production)
ENVIRONMENT=development

# Development user information (used when real auth is not available)
DEV_USER_ID=dev@local.dev
DEV_USER_EMAIL=dev@local.dev
DEV_USERNAME="Dev User"

