# ws4e — plan corrections

**This file OVERRIDES `2026-08-25-ws4e-surfaces-and-gates.md` wherever the two disagree.**
Every task dispatch points here alongside the plan. Written before Task 1, extended during
the build.

Precedent: ws4a found six corrections, ws4b twenty-three, ws4c four blocking plus sixteen
anchors, ws4d five blocking plus twenty-seven anchors.

---

## 1. The round-1 review findings NEVER applied to this plan

`ws4-START-HERE.md` §3 warns that roughly seven of ws4e's twenty-nine round-1 findings were
never applied, and that they are the only known-unfixed items in the set. The plan absorbed
the rest — E2b exists for finding 6, the sabotage in E4 is finding 4's corrected version, E3
carries finding 5's third mechanism, the baseline artifact replaces finding 9's `/tmp` path,
the deleted-test regex is finding 8's fix, and E1's `showSpec` ruling is finding 19's.

These are the ones the plan text does **not** answer. Each carries a ruling, because ws4e has
no successor to hand them to.

| # | Finding, verbatim in substance | Ruling |
|---|---|---|
| **1** | **E2 has no read path. Findings cannot reach the frontend.** `findings_from_record(record, content_hash) -> list[Finding]` is defined by ws4b and called by nobody | ~~**E2 owns closing it**~~ — **STALE. The finding is CLOSED and this ruling is reversed.** Re-measured by the controller after probe A contradicted it: `findings_from_record` is imported at `session_manager.py:31` and called at `:218` inside `_deck_findings`, which feeds a `findings` key on **all three** `get_slide_deck` read paths (`:1827`, `:1916`, `:1943`), and `SlideDeck.findings?: SlideFinding[]` is declared in `frontend/src/types/slide.ts` with a docstring stating it is emitted on all three paths. **The server half of E2 is already wired.** E2 is a frontend-only task: stop rendering `testFindings` and render `slideDeck.findings`. **An implementer told to build a read path would duplicate or break the live one** |
| **18b** | The parenthetical half of finding 18: **E1's "never compiled style content" assertion is unfalsifiable at the UI layer**, because `DesignContractRef` holds three integers. There is no compiled style content in the object to leak | **Reshape it, do not drop it.** An assertion that cannot fail is the class this programme has paid for five times. E1 asserts instead that the rendered spec view contains **no CSS-shaped text** — no `var(--`, no `{`…`}` rule body, no `@media`/`@font-face` — sourced from a `DeckSpec` fixture whose ids resolve to a design system that *does* have compiled content. That can fail: widen the component to render the resolved style and it goes red |
| **22** | **§G1's discoverable target is dropped.** The spec requires "a dedicated directory **plus a `make test-agentic` (or equivalent script)** … Discoverable and one command, rather than a marker nobody remembers." E3 specifies the directory, the marker, the guard and the disabled job — no command | **E3 owns it.** Add the command in whatever form this repo already uses for such entry points; probe B reports the existing convention (Makefile target versus `scripts/`). A one-command entry point is a spec requirement, not a convenience |
| **23** | **E4 restates ws4b's tests without saying what it adds**, and the genuinely untested case is **human-versus-graph**: the graph bumps the deck `version` twice per turn while the frontend holds `slideDeck.version`, so a user editing mid-turn gets 409s. E4 tests machine-versus-machine only. Row 2's 409 is also route-level, in a suite described as needing "a database, no model" — it needs the app too | **E4 owns it, and gains a row.** Keep rows 2 and 3 (their added value is concurrency against a real Postgres, which ws4b's tests do not have) and add the human-versus-graph case as the row that is genuinely new. The 409 is asserted through the route with a test client — say so in the test's docstring rather than pretending the suite is app-free |
| **26b** | The second half of finding 26: **`tests/integration/test_export_parity.py`, the existing PRD §3 export-parity suite, is in no CI job** | **Ticket, not ws4e** — unless probe B finds it inside the set of integration files a job already names, in which case the finding is stale. ws4e's E6 covers export parity manually on a deployment; adding a job for a pre-existing uncollected suite is scope this plan does not carry, and ws4a owned collection hygiene |
| **13b** | The parenthetical half of finding 13: **`_reassign_new_objects_to_shared_owner` is not checkable from RUNNING either** | **Accept.** E6 check 1 gains an observable for the migration that does real work (the `ConfigPrompts` DROP); the shared-owner reassignment is index finding #12's, assigned to ws4b, and no ws4e check depends on it |

**Ruling E-1: findings 1, 22 and 23 are BLOCKING and are absorbed into their owning task's
brief.** 18b is blocking for E1's test only. 26b and 13b are recorded and not acted on. Cost
if wrong: 1 would ship a drawer that renders nothing in production while its tests pass off a
fixture — the single most expensive shape here, and the reason it is ruled blocking rather
than deferred.

---

## 2. Execution order — SEVEN tasks, and they are SERIAL

`.github/workflows/test.yml` is touched by E1, E2, E2b, E3 and E4, and `AppLayout.tsx` by E1,
E2 and E2b. Two agents editing one file concurrently is the conflict this project has already
paid for, so there is no parallelism to win here. ws4d was wholly serial for the same reason.

**E1 → E7 → E2 → E0 → E2b → E3 → E4 → E5 → E6.**

**NINE tasks, not seven.** Two were added after the plan was written, both by measurement and
both assigned to ws4e by name in `ws4-FINAL-PR-HANDOVER.md`:

- **E0 — render the incremental slide event.** §6 below. Handover §3b's first item; E2b's flag
  has no operands without it.
- **E7 — the CI hang.** §11 below. Handover §4.1 (continuous integration) is ws4e's, and the
  first run ever made on this branch found `integration-graph` hanging on a runner. **Blocking:
  no Definition-of-Done item resting on a CI observation can be claimed until it is fixed.**
  Placed second because everything after it wants a CI signal to land against.

Two ordering constraints that are not preference:

- **E5 after E3**, because E5's six layer-3 RC tests live under `tests/agentic/`, which E3
  creates along with both its guards and its disabled job.
- **E2 after E1**, because E1 adds the `deck_spec` field and the `DeckSpec` TypeScript mirror
  to `frontend/src/types/slide.ts`, and E2 edits the same file for the findings read path.

E6 is the controller's, not an implementer's: it is a deployment and seven manual checks.

---

## 3. Standing instructions for every task dispatch

Carried into every brief, because each one is a defect this branch already paid for.

1. **Never `pip install`.** Shared pyenv site-packages; an install corrupts sibling agents'
   runs. Interpreter is `~/.pyenv/versions/3.11.0/bin/python`.
2. **No git commands, not even read-only ones.** A `git show` in this shared tree destroyed a
   sibling agent's uncommitted work and cost ~15 applied findings. The controller commits.
3. **Re-derive every anchor by grep before using it.** Anchors drift *during* a build, not
   only between them: inside ws4d alone `poll_chat` moved `:669` → `:691`, `restore_version`
   `:2222` → `:2493`, `duplicate_slide` `:2850` → `:3353`. A reviewer's correction of an
   anchor is not evidence — `get_system_client()` is at `agent_factory.py:67`, the plan says
   `:56`, a review "corrected" it to `:57`, and both are docstring prose.
4. **Locate constructs by a distinctive bare TOKEN, never a paren-anchored prefix.** This
   codebase passes functions by reference, so `mark_dirty(` finds nothing where `mark_dirty`
   finds it. Three agents including the controller have made that exact mistake.
5. **Sabotage-verify every guard, and verify the sabotage landed on the executed path.** A
   sabotage that misses its target is indistinguishable from a test that cannot fail. **Read
   sabotage results by which tests fired, never by how many.**
6. **Adding a paired assertion and verifying the pair can fail are SEPARATE steps.** Three
   steps: add the guard, pair it, then sabotage the pair and read which assertion fired.
7. **Commit before sabotaging.** Restoring a file to its committed state to undo a sabotage
   also wipes uncommitted work in that file; that produced a false self-report on ws4d twice.
   Revert a sabotage by targeted edit, and re-read every claim from the committed blob.
8. **When the defect already ships, write the test RED FIRST** and confirm it fails before
   fixing anything. §4.6's inert re-review had fourteen green tests over it precisely because
   nobody did.
9. **`chat_service.py:32` gates COLLECTION of the entire unit suite** via
   `tests/unit/conftest.py` → `session_manager` → `src/api/services/__init__.py`. Reshape it
   and the suite stops collecting rather than failing.
10. **Ruling R2 binds and this is the last chance to break it by accident.**
    `src/services/agent.py`, `src/core/prompt_modules.py` and
    `src/services/design_system_compiler.py` are **never modified** — read and import freely,
    private names included. The two recorded exceptions (ws4a's `merge_css`, ws4c importing
    `UNTRUSTED_DATA_NOTICE`) are not widened.
11. **Frontend type-checking is `npm run typecheck`, never `npx tsc --noEmit`** —
    `frontend/tsconfig.json` is a solution file, so `--noEmit` checks zero files and exits 0.
12. **Report failure CAUSES, never counts**, and a count that will be acted on is measured
    without a `head`, a `sort -u` or a `set()` between the data and the number.
13. **No layer-3 assertion may be weakened to accommodate a placeholder prompt.** The skills
    ship substantive but un-prompt-engineered instruction text by design. If an assertion is
    tempting to weaken, skip the test and record why. A skipped honest test beats a passing
    dishonest one.

### The six fake-guard shapes, from `.ws4d-PLAN-CORRECTIONS.md`

Every one was found by sabotage, never by reading. A test of each shape passes when:

| Shape | Passes when… |
|---|---|
| absence-assertion (§18) | the code never ran at all |
| tautological-default (§22) | the code ran but carried nothing |
| mixed shape/behaviour (§24) | a *different* assertion in the same test fired |
| inert pair (§26) | the paired direction is satisfied by something else |
| involution (§29) | the transformation applied twice is the identity, so the defect cancels |
| unreachable stub (§31) | the stubbed outcome is one production can never produce |
| self-comparison (§32) | both sides of the comparison are the same object and move together |

> **"The check found something" is far weaker than "the check found what it should have found."**

**§31's diagnostic, which is the one to apply here:** ask what *real* input would produce the
outcome your stub returns. If you cannot name one, the test is measuring your fixture rather
than the code.

---

## 4. The baseline, measured at the cut

`7 failed, 5484 passed, 10 skipped, 1 xfailed`; `--collect-only -q` reports **5501
collected**. FOUR environmental causes, and the skip count is a **range** (9 or 10), not a
number, because the dead Genie space moves between the failed and skipped columns.

Full cause table and the two deploy-autoscaling occurrence counts are in the ledger at
`.superpowers/sdd/2026-08-25-ws4e-surfaces-and-gates/progress.md`.

**The plan's own DoD gap, recorded because ws4e is the last workstream that can set the
precedent:** the baseline artifact records skips as a **count** and never enumerates them, so
a test silently becoming skipped is invisible to a by-cause comparison. E6's final comparison
enumerates skipped ids as well as failures.

---

## 5. Probe findings

Sections 6 onward are written from the five pre-pass probes (frontend surfaces, CI and test
topology, the RC markers, concurrency and inherited items, the release gate). They are the
per-claim corrections and the anchors, and they override the plan. Full evidence for each
lives in the probe files under
`.superpowers/sdd/2026-08-25-ws4e-surfaces-and-gates/probe-{A..E}-*.md`.

---

## 6. BLOCKING — the plan is MISSING A TASK: nothing renders `slide_ready`, so E2b's formula has no operands

**Measured by the controller, after probe A contradicted the plan.**

E2b states its flag is *"exact, not a heuristic"* and that *"the client already holds
everything required"*:

```
reviewInProgress = releasedPositions.size === deckSpec.slides.length && !turnComplete
```

**Neither operand exists.**

- `handleStreamEvent` in `frontend/src/components/ChatPanel/ChatPanel.tsx` has exactly
  **seven** `case` branches — `assistant` `:185`, `tool_call` `:198`, `tool_result` `:217`,
  `session_title` `:231`, `session_created` `:237`, `error` `:245`, `complete` `:252`. **There
  is no `slide_ready` case.** `frontend/src/services/api.ts:82` says so in its own words: the
  frontend *"has no `slide_ready` case yet and falls through its default path."*
- `grep -rn 'releasedPositions\|released_positions' frontend/src/ src/` returns **nothing**.
- `SlideDeck` in `frontend/src/types/slide.ts` has **no `deck_spec` field** (E1 adds it).

So the flag as specified always evaluates `0 === N`, and its headline test — *"the flag turns
on once every position has been released"* — **can never go green**. Weakening it to make it
pass is the exact failure this plan refuses elsewhere.

**Ruling E-2: add a task, E0, and run it before E2b.** This is not scope creep — it is the
first of the four items `ws4-FINAL-PR-HANDOVER.md` §3b assigns to ws4e by name:
*"Frontend rendering of the incremental slide event. ws4d delivers both backend transports and
the TypeScript union member; ws4e realises the user-visible feature."* The plan has no section
for it because §3b's list was written after the plan. E0 adds the `slide_ready` case and the
released-position state E2b then reads.

Two facts that will bite E0, both from ws4d:

- **Seven case branches, not two** — a duplicate `case` is a TypeScript compile error.
- **The TS union member is guarded by a test that reads `api.ts` as TEXT**, because types are
  erased and deleting the member left `npm run typecheck` at exit 0. Do not "improve" that
  test into a type-level one.

Cost if wrong: E0 is the difference between shipping the incremental-delivery feature two
workstreams built the transports for, and shipping two transports nothing consumes.

**Revised order: E1 → E2 → E0 → E2b → E3 → E4 → E5 → E6.**

---

## 7. Frontend corrections (probe A)

| Plan says | Truth | Consequence |
|---|---|---|
| `frontend/src/services/seenState.ts` | **`frontend/src/components/SlideViewer/seenState.ts`** | a test following the plan's import path does not compile |
| `_require_slide_permission` at `src/api/routes/slides.py:87` | **`:129`**; `:87` is inside `InsertSlideRequest`'s **docstring**, and the route decorator is at `:109` | an E1 assertion aimed at `:87` lands in dead prose. Same class as ws4d's `get_system_client()` trap, where the plan, a review's "correction" and the reviewer were all quoting docstrings |
| E2b needs only client state | needs **E1's** `DeckSpec` type and `deck_spec` field first | writing E2b's tests before E1 lands gives TypeScript errors, not red tests |
| E2 must build a findings read path | **already wired** — see §1, finding 1 | building it again duplicates or breaks a live path |

`AppLayout.tsx`'s fixture wiring, the remount-hazard comment, `testFindings` /
`window.__TELLR_TEST_FINDINGS__`, `SlideViewer.tsx`'s `dismissed` reset-on-`deckKey`, and
`mockFindings.f3` at `slideIndex: 3` are all **confirmed as the plan describes**. Re-derive
their anchors at dispatch time; do not copy them forward.

---

## 8. BLOCKING — four CI guard tests the plan's new jobs will trip (probe B)

The plan adds two jobs (a disabled layer-3 job, an enabled layer-4 job) and three e2e specs.
This repo guards its own workflow file with unit tests, and **the plan satisfies none of
them**. Each of these fails the `unit-tests` job rather than the new job, so the symptom will
look unrelated to the change that caused it.

| # | Guard | What it demands | What the plan does |
|---|---|---|---|
| **BC-1** | `tests/unit/test_ci_collects_integration_tests.py` | every `tests/integration/test_*.py` is named as a **substring of a run block** | E4 says `pytest tests/integration -k layer4` — a **directory** invocation that names no file, so the new layer-4 files run nowhere *and* the guard fails |
| **BC-2** | `tests/unit/test_ci_test_summary_gates_every_job.py` | every job in `workflow["jobs"]` is gated or listed in `_GATE_EXEMPT` | a disabled `if: false` job **is still a job in the YAML**. E3 must add the layer-3 job to `_GATE_EXEMPT` |
| **BC-3** | `tests/unit/test_ci_exports_postgres_test_url.py` | every job with a postgres service exports **`TELLR_TEST_POSTGRES_URL`** | E4's job omits it, so `postgres_engine` **self-skips** and "Layer 4 passes" is claimed by a green skip — the precise shape of the CI-weaker-than-a-laptop defect ws4d fixed |
| **BC-4** | `tests/unit/test_ci_test_summary_gates_every_job.py` | a non-exempt job appears in test-summary's `needs:`, its **echo block**, AND the `for result in` failure loop — **three sites** | the plan mentions adding the job and enumerates none of the three, so the job either fails the guard or runs toothlessly |

**Ruling E-3: each of the four is an explicit acceptance criterion in its task's brief**, named
by guard file. Cost if wrong: BC-3 is the expensive one — it converts a load-bearing
concurrency suite into a silent skip while reporting green, which is exactly what this
programme's doctrine exists to catch.

**Round-1 finding 2 is STALE:** the `Frontend Unit Tests` job exists and passed on run
`35016156813` in 23s. ws4b delivered it. E1/E2/E2b still verify their own files appear in the
collected set — a job that exists is not proof a new file is collected by it.

---

## 9. The fifteen RC rules — meanings derived from the code (probe C)

**This section discharges the Definition-of-Done item** *"All fifteen RC rules have their
meanings derived from the code and recorded in `.ws4e-PLAN-CORRECTIONS.md`."* Full per-rule
evidence, with the surrounding code quoted, is in `probe-C-rc-rules.md`.

**Marker counts confirmed exactly** as the round-1 review recorded them, and re-measured by
the controller without a `head` or a `sort -u` between the data and the number:
`chat_service.py` **61**, `agent.py` **14**, `mcp_server.py` **1**; distinct markers are
**RC1..RC15**, no gaps.

**Every meaning in the plan's table is CONFIRMED. Fourteen of the fifteen ANCHORS have
drifted** — by 150 to 600 lines. Only RC5's holds. This is the plan's own warning about
wrong-but-plausible mappings landing the other way round: the semantics were right and the
addresses were stale.

| Rule | Meaning, read off the code | True anchors | Layer |
|---|---|---|---|
| RC1 | validate the response is real slide HTML, retry once | `agent.py:979`, `:1447`, `:1703` | — builder integrity, no test |
| RC2 | reuse `_is_add` from early detection | `chat_service.py:735`; `agent.py:939`, `:951`, `:1195`, `:1390`, `:1540`, `:1644`, `:1792` | **3** |
| RC3 | guard: `slide_context` was provided but parsing failed | `chat_service.py:707` (sync), `:1461` (streaming) | **CI** |
| RC4 | unique canvas IDs to prevent collisions | `agent.py:1024`, `:1166` | — builder integrity, no test |
| RC5 | validate and attempt to fix JS syntax errors in slide scripts | `src/utils/js_validator.py:3` — **the only anchor that held** | — builder integrity, no test |
| RC6 | the deck cache survives backend restarts | `chat_service.py:692` (sync), `:1443` (streaming) | **CI** |
| RC7 | log script status before and after | `chat_service.py:742`, `:790` (sync), `:1599`, `:1645` (streaming) | — logging only, no test |
| RC8 | synthesise `slide_context` from a parsed reference | `chat_service.py:1488`, `:1494`, `:1499`, `:1518`, `:1528` — **streaming only, no sync counterpart** | **3** |
| RC9 | add *with* a slide reference | `chat_service.py:1551`, `:1562` — streaming only | **3** |
| RC10 | edit intent WITHOUT a slide reference → clarify | `chat_service.py:564`, `:600` (sync), `:1143`, `:1188`, `:1491`, `:1539`, `:1541`, `:1660`, `:1675` (streaming) | **3** — and it is **E3's own first layer-3 row**, the same test, not a tenth |
| RC11 | conflict between UI selection and a text reference | `chat_service.py:892`, `:916`, `:927`, `:932` (sync), `:1293`, `:1317`, `:1326`, `:1335`, `:1761` (streaming) | — **precondition retired**, no test |
| RC12 | generation intent with an existing deck → ask add-or-replace | `chat_service.py:568`, `:571` (sync), `:1150` (streaming) | **3** |
| RC13 | auto-create `slide_context` from a text reference | `chat_service.py:631`, `:634`, `:655` (sync), `:1218`, `:1221`, `:1242` (streaming) | **3** |
| RC14 | frontend/backend deck-state mismatch | `chat_service.py:1258`, `:1266` — streaming only | **CI** |
| RC15 | canvas-ID rewriting | `chat_service.py:3168`, `:3193` | — builder integrity, no test |

**RC11's precondition is confirmed retired**: `SelectionContext` is absent from
`frontend/src`, and both surviving `slide_context` producers carry fixed messages with no
textual slide reference. Its conflict branch is unreachable through the product, so under
ruling R1 it gets no test.

**The plan's layer split is confirmed against the code**: CI for RC3, RC6, RC14; layer 3 for
RC2, RC8, RC9, RC10, RC12, RC13. One caveat worth recording, because it is the kind of thing a
later reader would otherwise "fix": **RC12's early clarification path is mechanically
deterministic today** — it fires pre-LLM off a regex — so a CI test of it is *possible*. It
stays layer 3 anyway, because a CI test would pin the **old** guard rather than the
architect's intent handling, and pinning the retired mechanism is how a rewrite ships the
regression green.

### RULING E-4a: existing RC tests are the MONOLITH's and are neither reused nor duplicated.
Probe C found RC1–RC6 covered in `tests/unit/test_slide_editing_robustness.py`, RC14 in
`test_add_position_bug.py::TestRC14StateValidation`, RC15 in
`test_chart_persistence.py::TestRC15OptimizeScriptPreservation`, and RC10 partially in
`test_mcp_server.py`. Those pin the monolith, which R2 forbids modifying and which no ws4e
test may repoint. E5's nine tests are **new tests of the architect's language handling on the
graph path**. Cost if wrong: an implementer who "notices RC3 is already tested" claims a DoD
item off a test that cannot see the graph.

---

## 10. Concurrency and the inherited ledger (probe D)

Every item re-measured against the code. Where a handover and the code disagree, **the code
wins and the handover is corrected here**.

### 10a. E4's six assertions — all six are buildable, with these mechanics

| Assertion | What it actually contends on |
|---|---|
| 15 parallel row writes | `write_slide` opens **its own session** (`slide_repository.py:137`); contention is the composite primary key `(session_id, position)` (`session.py:434`), nothing deck-level. **Only load-bearing on Postgres** — SQLite's single-writer lock hides it |
| the deck `version` counter rejects a stale write with 409 | **CORRECTION to the handover.** There are **FOUR** production call sites of the deck-level writer, not two: `tour.py:114`, `chat_service.py:3830`, `nodes.py:1699`, `nodes.py:2692`. **None passes `expected_version`.** The live 409 comes from a *different* writer, `save_slide_deck` — so a test that asserts the 409 is testing that other path unless it targets the deck-level writer deliberately |
| two deck-level writes in one turn | they are `architect_node` (`nodes.py:1699`) and `deck_reviewer_node` (`nodes.py:2692`), and they are **sequential**, not concurrent. So the real assertion is the **double `version` bump within one turn**, which is also what makes finding 23's human-versus-graph case bite |
| the release query from a different process | `slides_since_cursor` (`session_manager.py:2099`) reads **rows only**, with zero caches (`lru_cache` grep is empty). **Do not use `releasable_positions`** — it reads GRAPH STATE, not rows, and would make the cross-process test meaningless |
| the checkpointer resumes in a second process | `SqlAlchemyCheckpointSaver` (`checkpointer.py:203`) goes through the **shared engine** by design (`:236`) and never holds its own connection — which is the entire reason E6 check 6 can observe an expired token. **`thread_id` IS the `session_id`** (`builder.py:269`); no `checkpoint_id` plumbing needed |
| four concurrent sweepers run at most one arc review | the outer lease predicate `.where(unclaimed)` **is** at `spec_sync.py:479` — the handover's anchor holds. `.limit(1)` is at `:472`. No `with_for_update` or `skip_locked` anywhere in the file |

**Two extra traps for whoever writes these:**

- **`postgres_engine` self-skips SILENTLY.** A layer-4 test that never runs looks identical to
  one that passed. This is BC-3's twin: assert inside the suite that it is running against
  Postgres, don't infer it from a green tick.
- **`graph_turn_env` (`conftest.py:700`) monkeypatches, and monkeypatches DO NOT CROSS A
  PROCESS BOUNDARY.** The cross-process tests cannot use it for the child process.
- Use `postgres_engine` (`conftest.py:235`); `sqlite_engine_file_backed` is **`StaticPool`**
  (`conftest.py:100`) and corrupted the database 3 of 3 runs on ws4c. **The pool class is the
  load-bearing part, not the storage location.**

### 10b. Sweeper throughput — the over-claim is in the code's own docstring

`skip_locked` raises the throughput **floor** from 1 to 4; the **ceiling stays 4**. The
handover's "1–4 decks per minute rather than 4" is right, and **the over-claim is at
`spec_sync.py:710`, in the code's own docstring** — so fixing the code without fixing that
line leaves a false claim in the tree.

### 10c. Corrections to the inherited ledger

| Handover says | Truth | Disposition |
|---|---|---|
| **"nothing calls `delete_thread`"** | **WRONG.** It is called at `chat_service.py:2113`, from `clear_context`, route-reachable at `sessions.py:732`. The real gap is different and narrower: **that user action is its ONLY caller — there is no automatic retention policy** | **Re-file, do not "fix".** Ruling E-5 below |
| `resolved_data` is not a re-review trigger; "the fix is one line" | the field tuple genuinely omits it, and `source_contradiction` is `objective=True` and already counts as failing — **but it is UNREACHABLE on the graph path** because `ResolvedData.figures` is always `[]`. `nodes.py:1730-1737` says so in its own docstring, and adds that closing it *"needs a declared field on `AnalystOutput`, an escalation to ws4b, not a mapping invented here"* | **Ruling E-6 below** |
| `_otel_bootstrap.py` is in `src/core/` | it is **`src/api/_otel_bootstrap.py`** | anchor corrected |
| three per-row INFO lines at `:82`, `:86`, `:165` | **only `:165` is per-row.** `:82` and `:86` are **per-session** skip returns | the startup-timeout worry is a quarter the size it looked |
| `setup_logging()` has no caller | **CONFIRMED** — `src/utils/logging_config.py:87` defines it, `src/utils/__init__.py:17` re-exports it, and nothing calls it repo-wide | Ruling E-7 below |
| the emitted-spec fix is "the same set comparison already written" | **the proposed fix is WRONG.** Set equality contradicts `nodes.py:870-876` and would **reject legitimate deck growth** — a build turn that adds slides is not a defect. Needs a **superset-plus-contiguity** check in a new helper | **Ruling E-8 below** |
| the `mark_dirty`-versus-claim race is banked `xfail(strict=True)` | **CONFIRMED** at `test_claim_exclusivity_postgres.py:552-553`. Consequence to state plainly: **landing the sweeper-throughput fix XPASSes it, and a strict xpass reddens CI as a NEW cause** | it is a tripwire on the item E4 is most likely to touch |
| checkpoint growth is unbounded | **CONFIRMED and worse than stated:** `put` stores **one whole-state blob per checkpoint** (`checkpointer.py:383`) and `findings` is `operator.add` with no cap (`state.py:271`), so growth is **super-linear in turns**, not linear | Ruling E-5 |
| `architect_message` inherits across turns | **CONFIRMED** no reducer (`state.py:198`) — but unlike `findings` it is **one overwritten slot**, and the inheritance is masked because both `invoke_graph` seeders write it and `nodes.py:1404` is unconditional | **Accept.** It cannot be observed changing behaviour today |

### The rulings on the inherited items

**Ruling E-5 — checkpoint growth and `delete_thread` are a TICKET, not ws4e work.** The
handover offered "ws4e or ticket". `delete_thread` exists and works; what is missing is a
retention *policy* — when a thread may be discarded without destroying a conversation a user
may still resume. That is a product decision with a data-loss failure mode, not a test.
**Cost if wrong:** production Postgres grows super-linearly per session until someone sets a
policy. Recorded in the handover as a named ticket rather than closed silently.

**Ruling E-6 — `resolved_data` is NOT added to the trigger tuple.** The one-line change is
**inert**: the criterion it would enable cannot fire, because `figures` is always empty and the
code says closing that needs a ws4b schema field. Adding the line would ship a trigger that
looks live, tests green, and does nothing — a tautological-default guard (§22) manufactured on
purpose. **Cost if wrong:** when `AnalystOutput` later gains the field, someone must remember
this line. Mitigated by naming it in the handover next to the ws4b escalation.

**Ruling E-7 — production logging: DECIDED, and the decision is "leave it off, and say so."**
The handover required a decision in writing either way. `setup_logging` has no caller, so
`mark_dirty`'s audit line — the only record of which human dirtied a deck — **goes nowhere
today**. Switching it on is a one-line change whose blast radius is every log line in a
four-worker production app, on a branch that has never run in CI until today and whose deploys
have previously been blocked by startup timeouts. **So: not in ws4e.** What ws4e owes instead
is the truth in writing — that the human-edit audit trail **does not exist** — recorded in the
handover as a named ticket with the one-line fix and the `:165` per-row caveat attached.
**Cost if wrong:** the audit trail stays absent for another release. Turning it on blind and
timing out a deploy costs more.

**Ruling E-8 — the architect-emitted spec check is a TICKET, and the handover's proposed fix is
struck.** Set equality would reject legitimate deck growth, so the cheap fix on offer is not
merely insufficient — it is a new defect that would fire on correct decks. The honest shape
(superset plus contiguity, in a new helper, on a build turn where rows exist) is a design
change to a guard on the user's build path, and ws4e's remaining budget is the release gate.
**Cost if wrong:** a model echoing back `current_deck_spec` can re-materialise a deleted slide
on a build turn. It is user-present, a save point exists per turn, and restore works — visible
and recoverable, which is why it is a ticket and not a blocker. **The struck fix matters more
than the deferral:** without this ruling, someone reads "the cheapest shape is the same set
comparison already written" and ships it.
