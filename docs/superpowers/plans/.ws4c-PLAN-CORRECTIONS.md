# ws4c — plan-vs-code corrections pre-pass

**Written 2026-09-13, before Task C1.** Branch `feat/ws4c-graph-core`, off `feat/langgraph-core`
@ `3ee13c73` (ws4b merged at `70009b7e`).

**THIS FILE OVERRIDES THE PLAN** (`docs/superpowers/plans/2026-08-25-ws4c-graph-core.md`) wherever
the two disagree. Every task dispatch is pointed at it.

Provenance is marked on every item:

- **[measured]** — the controller ran the command and read the output.
- **[scout]** — a read-only scout reported it and pasted evidence; not independently re-run.
- **[scout, re-probed]** — a scout reported it and the controller re-ran the check.

Bulk evidence lives in the gitignored session workspace
`.superpowers/sdd/2026-08-25-ws4c-graph-core/scout{1..6}-report.md`. Everything load-bearing is
copied into this file, because that workspace is deleted when the plan completes.

---

## 0. Environment, re-measured on this branch

| Fact | Value |
|---|---|
| Interpreter | `~/.pyenv/versions/3.11.0/bin/python` — shared pyenv site-packages. **Never `pip install`.** |
| Files ws4c creates — all confirmed ABSENT | `src/services/graph/`, `src/core/skills/`, `src/services/foreman_service.py`, `src/services/agent_resolution.py`, `src/services/template_sections.py`, `src/utils/graph_safety.py`, `tests/integration/conftest_stub_skills.py` [measured] |
| ws4b artifacts ws4c consumes — all PRESENT | `src/core/checkpointer.py`, `src/api/services/deck_level_writer.py`, `src/services/deck_review_store.py`, `src/services/deck_css_aggregator.py`, `src/domain/{finding,deck_spec,skill_io}.py` [measured] |
| `.claude/worktrees/` | nine stale repo copies; every repo-wide grep must exclude them or scope to `src/ tests/ frontend/src/ scripts/ .github/ packages/` |
| `.env` contamination | `src/core/database.py` calls `load_dotenv()`; an explicit env var overrides it. Measure anything touching a Databricks client with the CI env. |

## 1. THE BASELINE — re-derived on this branch before any edit

Command: `~/.pyenv/versions/3.11.0/bin/python -m pytest tests/ -n auto -q`, no app server
running. [measured 2026-09-13]

    3 failed, 4578 passed, 8 skipped, 332 warnings in 191.35s
    COLLECTED: 4589   (pytest tests/ --collect-only -q)

**Identical to `docs/superpowers/baselines/pr3_ws4_collected.log` Section 2b — same three tests,
same four assertion strings, all four re-measured individually rather than grepped as one.** The
pre-change baseline therefore needs no correction; ws4c must still ADD **Section 2c** with its own
post-change measurement, per that file's own re-derivation rule (C8 moves a module and C7 re-homes
two security controls).

CAUSE 1 — deploy-autoscaling (reproduces anywhere). **Two distinct assertion strings; never grep
for only one:**

    tests/unit/test_deploy_autoscaling.py::TestGetOrCreateLakebase::test_returns_autoscaling_when_available
      E  AssertionError: assert 'provisioned' == 'autoscaling'
    tests/unit/test_deploy_autoscaling.py::TestGetOrCreateLakebase::test_falls_back_when_autoscaling_creation_fails
      E  AssertionError: Expected '_get_or_create_lakebase_provisioned' to have been called once. Called 0 times.

CAUSE 2 — stale stored Genie space id (ENVIRONMENTAL, local `.env` dependent):

    tests/integration/test_genie_integration.py::test_genie_conversation_continuation
      E  databricks.sdk.errors.platform.NotFound: Space with id 01effebcc2781b6bbb749077a55d31e3 not found
      E  src.services.tools.genie_tool.GenieToolError: Failed to query Genie space after 3 attempts: ...

Clause 3 of the gate ("no test stopped existing") **cannot be checked by a count.** Diff the test
tree for removed `def test_` / `async def test_` / `class Test` lines and for deleted files.

---

## 2. BLOCKING — a conditional-edge router SEES the writes of the node it hangs off. C4 as written builds nothing.

**[measured]** Probe, on the installed langgraph:

```python
def writer(state):
    print("NODE sees marks =", state.get("marks"))
    return {"marks": {"a": 1}, "log": ["node ran"]}

def router(state):
    print("ROUTER sees marks =", state.get("marks"))
    return "end"

g.add_conditional_edges("writer", router, {"end": END})
```

Output:

    NODE sees marks = {}
    ROUTER sees marks = {'a': 1}
    ROUTER sees log   = ['node ran']

**Why this breaks C4 exactly as drawn.** C4 has `foreman_node` write
`dispatched_at = scoped(turn_id, {p: now})` for "the positions it actually dispatches", and then
`foreman_router` decide the batch. But `next_dispatch_batch` excludes in-flight positions
(C2 rule 2), and by the time the router runs, the node's stamps have landed — so every position
the node just stamped now reads as in-flight, the router's `next_dispatch_batch` returns `[]`,
the turn falls through to `END`, and **the graph builds nothing while every C2 unit test stays
green.** That is precisely the "scheduler test passes while the shipped topology degrades" failure
spec §8 warns about, arriving through a channel neither C2 nor C5 names.

**Ruling C-1: `foreman_node` decides the batch, and `foreman_router` reads the decision out of
`foreman_wakes` — it MUST NOT call `next_dispatch_batch`.**

`foreman_wakes` is already the declared channel for exactly this: C4 requires a wake recorded on
**every** foreman entry, including one that dispatches nothing (`[]`), and says the reconciliation
rule "reads the most recent entry". So:

- `foreman_node` computes the batch **once**, stamps `dispatched_at` for it, and appends it to
  `foreman_wakes` as `scoped(turn_id, [batch])`.
- `foreman_router` reads the **last** entry of `scoped_vals(state, "foreman_wakes")`. Non-empty →
  `[Send("builder", build_branch_payload(state, p)) for p in that batch]`. Empty → fall through to
  the fixer / placeholder / deck-reviewer / END ladder.
- The router calls `next_dispatch_batch` **nowhere**. A reviewer seeing it there should treat it as
  a defect.

Cost if wrong: the router and node could disagree about the batch. They cannot, because there is
exactly one producer of the decision and the router only reads it.

## 3. BLOCKING — `stalled_positions` limb 1 and C4's decision ORDER are mutually unsatisfiable. Reorder the node.

C4 states the foreman's precedence as `fix → stall → dispatch → all-committed → END`, and
`stalled_positions` limb 1 as *"has a `dispatched_at` entry, **is NOT in the most recent
`foreman_wakes` batch**, and is neither landed nor placeheld"*.

**Traced against the barrier, those two cannot both hold** [measured reasoning over the plan's own
runtime facts; no code exists yet to run]. Three-slide deck, position 1's builder dies without
placeholding:

- wake 1: `dispatched_at` empty → batch `[0,1,2]`, wake `[0,1,2]`, stamps `{0,1,2}`.
- barrier: the batch completes; 0 and 2 land, 1 does not.
- wake 2: position 1 has a `dispatched_at` entry, is not landed, is not placeheld — **but it IS in
  the most recent wake `[0,1,2]`, so limb 1 excludes it.** Limb 2 does not apply either (limb 2 is
  *"a `dispatched_at` entry with NO wake record in this turn"*). `next_dispatch_batch` returns `[]`
  because position 1 is outstanding-but-in-flight. Not all committed → **`END`, with a slide
  missing, no placeholder, no `error_state` and nothing in chat** — the exact outcome C4 forbids
  two paragraphs later.

The plan's own C2 test intent pins limb 1's wording (*"a dispatched, uncommitted position outside
the most recent wake's batch is stalled at zero elapsed time … while one inside that batch is
not"*), and C4 defends the exclusion at length as the guard that keeps the limb safe "if the
runtime ever does wake the foreman mid-batch". The terse precedence pseudocode is the weaker of
the two statements.

**Ruling C-2: keep `stalled_positions`' predicate verbatim and reorder `foreman_node`'s decision
so the stall check runs AFTER the batch is computed, found empty, and the empty wake recorded.**
The node's order becomes:

    1. has_pending_fix(state)                     -> "fixer",  wake [], stamp nothing
    2. batch = next_dispatch_batch(state)
    3. batch non-empty                            -> Sends,    wake [batch], stamp dispatched_at
    4. batch empty, stalled_positions(state')     -> "placeholder", wake [], stamp nothing
    5. all_positions_committed(state)             -> "deck_reviewer", wake []
    6. otherwise                                  -> END + error_state (see C4's own rule)

where `state'` is the state the router will see — i.e. with this entry's **empty** wake already
appended, which is what makes limb 1 fire on positions left over from the completed batch.
`foreman_router` mirrors this ladder, taking the batch from the last wake per Ruling C-1 and
never recomputing it.

Consequence, and it is a behaviour choice worth stating: **a stalled position is placeheld one
wake later than the plan implies** when there is still unstarted work — the foreman dispatches the
real work first and reconciles when the queue drains. That is strictly better than placeholding
while builders are available, and it costs one extra superstep.

Cost if wrong: on a runtime that woke the foreman mid-batch, a still-running position would be
excluded from limb 1 (safe) rather than placeheld (unsafe) — the exclusion is preserved, so this
ruling makes the limb strictly safer than dropping it, and reachable, which it was not.

## 4. CONTRACT — `deck_review_store`'s signatures differ from the plan (ws4b §19, already decided)

C4's `architect_node` row says *"Read the previous arc verdict via `get_deck_review(session_id,
deck_id)`"* and *"persist findings via `save_deck_review`"*. **Neither signature exists.** ws4b
froze [ws4b corrections §19, ruling R-O]:

    ships as:   save_deck_review(db, deck_id, digest, findings, author=None) -> DeckReview
                get_deck_review(db, deck_id, digest) -> dict | None

**This is not an escalation — ws4b resolved it, with evidence, before anything bound to it.** The
call-site recipe, which both `architect_node` and `deck_reviewer_node` follow:

1. look up the `UserSession` from the `session_id` string;
2. call `SessionManager._get_deck_owner_session(db, session)` — it takes a **`UserSession` object
   and a `db`**, not a string;
3. take the owner's `SessionSlideDeck.id` as `deck_id`;
4. compute the digest from the committed slides with `compute_deck_digest`;
5. call the store.

A content-addressed getter must be told which digest to look up. For `architect_node` the digest
is the digest of the **currently committed** slides — i.e. what the deck looked like when the
previous turn's deck review ran, before this turn changes anything. Resolve it before committing
the new spec.

## 5. `DeckReviewOutput` declares NO verdict and NO message field

**[measured]** `src/domain/finding.py:236-239`:

```python
class DeckReviewOutput(BaseModel):
    """Output of the deck-level reviewer node."""
    findings: List[Finding] = []
```

C4 requires `deck_reviewer_node` to surface "the verdict to the human by persisting a chat
message — `add_message(session_id, role="assistant", content=<verdict prose>, ...)`". There is no
`verdict` and no prose field to read.

**Ruling C-3: compose the advisory text from `DeckReviewOutput.findings`** — a short lead line plus
one line per finding (criterion, message), and an explicit "no issues found" line when the list is
empty. Do **not** escalate to ws4b for a prose field: the findings carry everything the message
needs, and inventing a field is exactly what the freeze exists to stop.

## 6. `Finding.objective` is NOT validated against the registry — derive it, never trust the model

**[measured]** `src/domain/finding.py:173-188`: `Finding`'s only validator rejects an unknown
`criterion` and a `category` that disagrees with `CRITERIA`. **`objective` is unvalidated.**

So a reviewer whose model returns `objective=False` for `overflow` silently disables the entire
one-fix-round path for that finding, and one returning `objective=True` for `arc_gap` sends a
narrative finding to the fixer. Neither raises.

**Ruling C-4: `build_reviewer_node` and `fix_reviewer_node` re-derive `objective` from
`CRITERIA[f.criterion].objective` before branching on it**, rather than reading the model's value.
Use `f.model_copy(update={"objective": CRITERIA[f.criterion].objective})`. Test it: a review output
whose finding claims `objective=False` for `overflow` still routes to the fixer.

Note the corollary — `category` IS validated and a mismatch **raises inside `call_skill`'s parse**,
killing the turn. That is what C9's "the criteria block is generated from `CRITERIA`" buys.

## 7. The registry, measured, because three tasks branch on it

**[measured]** `src/domain/finding.py:70-147` — nine criteria:

| criterion | category | level | objective |
|---|---|---|---|
| `overflow` | design | slide | **True** |
| `contrast_failure` | design | slide | **True** |
| `rogue_colour` | design | slide | **True** |
| `distorted_image` | design | slide | **True** |
| `source_contradiction` | content | slide | **True** |
| `brief_not_delivered` | content | slide | False |
| `arc_gap` | narrative | deck | False |
| `cross_slide_repetition` | narrative | deck | False |
| `missing_conclusion` | narrative | deck | False |

Consequences the plan does not state:

- **Every deck-level criterion is subjective.** A deck finding can never reach the fixer, so
  `deck_reviewer_node` needs no fix path at all.
- `SlideVerdict = Literal["clean", "fixed", "surfaced"]` (`:44`) and `"placeholder"` is
  deliberately NOT in the union, so no reviewer node can mint a placeholder verdict.
  `commit_placeholder` writes `error: True` instead.
- `VERDICT_KEY = "tellr_review"` (`:50`), and findings are nested one level under it so
  `is_placeholder_record` never false-positives on a real review payload.
- **`build_verification_record(*, content_hash, findings, verdict)` already exists**
  (`:247-282`) and is **keyword-only**. Reviewer nodes build their `verification_record` with it;
  do not hand-roll the shape. `findings_from_record(record, content_hash)` (`:285`) is its inverse.

## 8. `make_finding_id`'s subject for a DECK-level finding

**[measured]** `make_finding_id(criterion: str, subject_hash: str, ordinal: int = 0)`
(`src/domain/finding.py:196`). The default `ordinal=0` is documented as a deliberate trap.

C4 states the slide case (`content_hash`). It does not state the deck case. **Ruling C-5: the
deck-level subject_hash is the deck digest** (`compute_deck_digest`'s output) — the same value the
`deck_reviews` row is keyed on, which makes the id stable while the deck is unchanged and new once
it is edited, exactly as the slide case does. `slide_index` is `-1`, per the field's own comment.

## 9. Deck-level findings and `FindingLevel` — decided deliberately, not discovered

ws4b's follow-up: `FindingLevel` is declared and never consumed; nothing checks that a deck-level
finding carries `slide_index == -1`; and `SlideViewer.tsx` filters findings by index, so a `-1`
finding is invisible and lands in the unseen set.

**Ruling C-6: ws4c's deck reviewer does NOT put deck-level findings into the `findings` state key
or into the read path's flat findings list.** They are persisted only to `deck_reviews` via
`save_deck_review`, and reach the human as the `info` chat message C4 already specifies. C1's table
already assigns `findings` to *"every `build_reviewer_node` and `fix_reviewer_node`"* and not to
the deck reviewer, so this is the plan's own division of labour made explicit.

Consequences: no `-1` ever reaches `SlideViewer`'s index filter from ws4c; `FindingLevel` stays
unconsumed by this PR; and surfacing deck findings in the drawer is ws4e's work, which owns the
drawer. Cost if wrong: ws4e adds drawer plumbing it already owns.

## 10. `design_contract` has NO `default_factory` any more — the guard is still right, its stated reason is not

C4: *"`design_contract` has a `default_factory`, and an all-`None` pydantic model is **truthy**, so
a bare truth test calls `resolve_template_bytes(None, None)` on every unpinned deck"* (round-3
finding 28).

**[measured]** `src/domain/deck_spec.py:108` — `design_contract: DesignContractRef` is a
**required** field with no default and no `default_factory`.

**The guard stands; only the justification is stale.** An explicitly-constructed
`DesignContractRef()` with all three ids `None` is still truthy, and the architect must always
supply the field, so a bare `if spec.design_contract:` is still always true. **Guard on
`design_contract.design_system_id is not None and design_contract.template_id is not None`**, as
C4 instructs. Do not go looking for a `default_factory` and conclude the guard is unnecessary.

## 11. The analyst's output has NO declared `GraphState` channel — it returns through `architect_message`

C1's `GraphState` table declares no analyst key, and undeclared keys are silently dropped. C4
requires `data_analyst_node` to "return exactly one of three outcome shapes" without saying where
it goes.

The superseded plan (`2026-08-24-pr3-langgraph-core.md:6684-6694`), which START-HERE names as the
reference for anything the split summarises, has it return `{"architect_message": out.synthesis}`
and a static edge `data_analyst → architect`. **[measured, quoted from that file]**

**Ruling C-7: the analyst returns through `architect_message`, and the edge back to the architect
is static.** `architect_message` is therefore written by TWO nodes — a correction to C1's table,
which attributes it to `architect_node` alone. It still needs **no reducer**: the architect and the
analyst never run in the same superstep. State that in C1's table comment so a later reader does
not "fix" it by adding one.

Related, and it removes an apparent gap: **`resolved_data` is not a `GraphState` key and does not
need to be.** `DeckSpec` carries `resolved_data: ResolvedData`
(`src/domain/deck_spec.py:109`), so `build_branch_payload` reads
`state["deck_spec"].resolved_data`.

## 12. `AnalystOutput` cannot populate `ResolvedData.figures` — a recorded gap, not a defect to work around

**[measured]** `ResolvedData(synthesis: str, figures: list[ResolvedFigure], gaps: list[str])`
(`deck_spec.py:65-68`) and `ResolvedFigure(key, value, source)` (`:59-62`), against
`AnalystOutput(outcome, synthesis, sources, gap, tried_tools, reason)` (`skill_io.py:122-137`).
**There is no field on `AnalystOutput` that maps to `figures`.**

So a graph-built spec's `resolved_data.figures` is always `[]`. That matters because
`source_contradiction` is *"only assertable against `resolved_data`"* — with no figures, that
criterion is unreachable on the graph path.

**Ruling C-8: record it as a known PR-3 gap; do not escalate and do not invent a mapping.**
`synthesis` and `gaps` are populated from `AnalystOutput.synthesis` and `.gap`; `figures` stays
empty. Closing it needs a declared field on `AnalystOutput` — an escalation to ws4b — which is a
scope addition, not a local edit. It sits alongside C9's two recorded gaps (brand assets and
user-uploaded images unreachable on the graph path). Note it in `data_analyst_node`'s docstring so
it is not read as an oversight.

## 13. C8's destination module is never named in ws4c. It is `src/services/agent_resolution.py`.

C8 says `agent_factory` "MOVES", that `agent_factory.py` "keeps every name as a re-export shim",
and that the DoD requires "all six `agent_factory` suites pass against **the moved module**" — but
ws4c's text never names the destination.

Resolved from the superseded plan's Task 5.2 (`2026-08-24-pr3-langgraph-core.md:7587-7745`), which
is explicit **[measured, quoted]**:

    **Files:**
    - Create: `src/services/agent_resolution.py`
    - Modify: `src/services/agent_factory.py` (re-export, keeping every public name)
    - Modify: six unit suites (repoint, never delete)

and gives the shim verbatim:

    # src/services/agent_factory.py — at the top, replacing the moved bodies
    from src.services.agent_resolution import (  # noqa: F401
        _build_tools, _design_system_is_active, _get_prompt_content,
        build_selected_template_block, build_type_scale_reassertion,
        extract_type_scale_block, get_template_for_generation,
        strip_type_scale_region_markers,
    )

**Ruling C-9: the destination is `src/services/agent_resolution.py`; `agent_factory.py` becomes a
re-export shim keeping every name, private ones included.** The shim list above is a starting point,
not the authority — the authority is the measured import inventory in §14, and every name any
suite or production module imports must be re-exported.

## 14. TASK ORDER — three inversions. Execute C1, C2, **C8, C3**, C6, C7, C4, C5, C9.

The plan's numbering is not a dependency order. Three inversions, each with a defect behind it:

1. **C8 before C3.** C8's own Sequencing note says `agent_resolution.py` *"is **created** here (C3
   adds to it)"*. Running C3 first also creates a circular-import window: C3's `resolve_slide_style`
   is told to import `_get_prompt_content` *"from `src.services.agent_factory`"*, and after C8
   `agent_factory` is a shim importing **from** `agent_resolution` — so `agent_resolution` importing
   back from `agent_factory` is a cycle. **With C8 first, `_get_prompt_content` is already local to
   `agent_resolution.py` and `resolve_slide_style` calls it directly, with no import at all.**
   C3's instruction to import it from `agent_factory` is therefore superseded.
2. **C6 before C4.** `build_branch_payload` calls `extract_section`, and `architect_node` calls
   `resolve_template_bytes`. Both are C6's.
3. **C7 before C4.** `builder_node` and `fixer_node` call `gate_emitted_html`; the fixer and
   reviewers call `spotlight_prior_slides`. Both are C7's.

C9 stays last: it only replaces prose bodies, and C5's layer-1 suite monkeypatches `call_skill`
entirely, so prose is invisible to it.

## 15. C3 and C9 both own `src/core/skills/`. C3 registers all seven with placeholder bodies.

C3's test intent asserts *"all seven skills load and bind to their `OUTPUT_SCHEMAS` entry; each
declares a version and a non-empty instruction body"*. That is unsatisfiable if C9 is the task that
creates the seven skills — and C4 cannot run a graph whose nodes call unregistered skills.

**Ruling C-10: C3 creates the loader, the frozen `Skill` dataclass, `call_skill`, and all seven
skill entries with short placeholder instruction bodies. C9 replaces those bodies with the copied
prose and owns the prose-specific tests** (no `88px`/`72px`/`56px`/`1280x720`; the image prose
rewritten with every tool instruction deleted; the grants table). §A1 already says the build
proceeds on placeholder prompts, so this is the plan's own premise applied to the ordering. C3's
"all seven load" test lands at C3 and must still pass at C9.

## 16. Tool grants: the architect declares NONE. C3's assertion is the one that changes.

C3's test intent asserts *"the analyst and architect declare tool grants and builders/reviewers
declare none"*. C9's prose table gives grants only to `data_analyst`, and says outright: *"Give the
architect its own grants row (its inventory/section work is tool-free, so the honest answer may be
none) or change C3's assertion — one of the two is wrong and whichever ships gets pinned by a
test."*

**Ruling C-11: `data_analyst` is the ONLY skill with tool grants; the architect declares `[]`.**
Spec §5.2.2's reason for grants at all is that the analyst is the OBO boundary where user-scoped
data access is concentrated; the architect's work — conversation, arc, section assignment — reaches
no tool. **C3's assertion is corrected to: `data_analyst` declares non-empty `tool_grants`, and the
other six declare `[]`.** Cost if wrong: one assertion and one skill's grants list change.

## 17. `deterministic_css` vs `token_css` vs `emitted_style_blocks` — what each holds

C4 says the pre-fan-out write's deterministic CSS is *"the pinned template's `token_css` plus its
`<style>` block"*; C1 declares `token_css` and `deterministic_css` as separate state keys, both
written by `architect_node` from `resolve_template_bytes`; C6 has `resolve_template_bytes` return
`(normalized layout_html, <style> block, token_css)`; and `emitted_style_blocks` holds "exactly one
element" on a pinned deck, from the same call.

**Ruling C-12, so three tasks agree on the bytes:**

| Key / column | Holds |
|---|---|
| `token_css` (state) | the template's `token_css` **alone**, verbatim from `resolve_template_bytes` |
| `template_layout_html` (state) | the **normalized** layout HTML, verbatim |
| `deterministic_css` (state) | `token_css` + the template's `<style>` block, concatenated. This is what the pre-fan-out write puts in the deck `css` column, and what `build_branch_payload` carries whole as the branch's section CSS (§M5, never pruned) |
| `emitted_style_blocks` (state) | a one-element list holding the template's `<style>` block **alone** — CSS text, **never `<style>`-wrapped markup** (`deck_css_aggregator`'s docstring: a wrapped block parses to error nodes and is dropped SILENTLY, no exception, no log) |
| unpinned deck | all four are empty / `""` / `[]`, and no `resolve_template_bytes` call happens at all (§10's guard) |

The post-commit write's `css` is `aggregate_deck_css(existing_css, emitted_style_blocks, token_css)`.
The template's block is therefore merged into a column that already contains it — which is
**correct and not a bug**: `merge_css` dedupes, and the second pass is what runs the
`ensure_deck_token_css` backstop over the knitted deck. Do not "optimise" the second write away.

## 18. `has_pending_fix` lives in `state.py`; `stalled_positions` lives in `foreman_service.py`

C1 describes both; C2's contract lists `stalled_positions` among the five foreman functions and
does not list `has_pending_fix`. C1's test-intent table tests `has_pending_fix` and does not test
`stalled_positions`; C2's tests do the reverse.

**Ruling C-13: `scoped`, `scoped_vals`, `_EMPTY_FOR`, the three reducers, `GraphState` and
`has_pending_fix` are C1, in `src/services/graph/state.py`. All five foreman functions — including
`stalled_positions` — are C2, in `src/services/foreman_service.py`.** C1 documents
`stalled_positions`' tombstone hazard; C2 implements it.

`_EMPTY_FOR` must therefore cover every key read through `scoped_vals`:
`landed_positions`, `placeheld_positions`, `reviewed_positions` → `set()`;
`slides`, `dispatched_at`, `fix_map`, `fixed` → `{}`;
`emitted_style_blocks`, `foreman_wakes` → `[]`.
(Plus `retry_count` → `{}` only if §19 keeps it.)

## 19. `retry_count` — DELETE the key.

C1 states the conflict itself: *"`retry_count` is declared with a reducer but **never written** — no
node retries anything (builder exceptions go straight to placeholder). Yet C2 tests state 'retry
priority' and 'retries appear ahead of higher unstarted positions'. Either delete the `retry_count`
key or define and implement its writer."*

**Ruling C-14: delete `retry_count` from `GraphState`.** No node in C4's nine retries anything — a
builder exception commits a placeholder — so a writer would be dead code, and a declared key no
node writes is precisely the "tested but unreachable" class C4 deletes `reviewer_router` for.

**C2's retry test is kept and re-stated as what it actually proves**, which is the plan's own
justification for it: *"Retries need no special case beyond clearing their in-flight marker: a
failed position is still outstanding, so ascending order re-enters it ahead of higher unstarted
positions by construction."* The test therefore asserts the **property**, with no `retry_count`
involved: given a state where a low position is outstanding with its in-flight marker cleared and
higher positions are unstarted, `next_dispatch_batch` returns the low position first. Name it for
what it pins (ascending re-entry), not for a counter that does not exist.

Cost if wrong: if a later PR adds real retries it declares the key then, with its writer, which is
the order this repo's rulings prefer everywhere else.

---

## 20. WRONG ANCHORS — sixteen of them. Use the right-hand column.

Every one re-derived by the controller after a scout reported it: **16 of 16 confirmed by
re-probe.** [scout, re-probed]

| The plan says | Reality |
|---|---|
| `resolve_agent_config` at `src/api/schemas/agent_config.py:238` | **`:198`** |
| `agent_factory.py:216` — `slide_style = style.style_content` | **`:218`** |
| `slide_repository.py:91` — the author write | `:91` is `deck_spec_slide:`; **`modified_by` is `:92`**. The columns are `created_by` **and** `modified_by`, both set from the one parameter (`:142-146`) |
| `session_manager.py:1549` — `"scripts": deck.scripts_content or ""` | **`:1696`**. `:1549` is `"slide_count": deck.slide_count,` |
| `.github/workflows/test.yml:102` — the `unit-tests` pytest line | **`:110`** |
| `integration-slides` job at `test.yml:306-345` | **`:314-357`** (`integration-genie` starts at `:359`) |
| `find_slide_roots` at `html_utils.py:72` | function **def is `:46`**; `:72` is the `soup.find_all(class_="slide")` line inside it. The plan intends to CALL the existing function — do not insert a second one |
| `_design_system_is_active` at `agent_factory.py:315-350` | **`:301-336`** |
| `get_verification_map` "is 66 lines" | **64 lines, `:1988-2051`** |
| safety-gate call sites at `agent.py:1488`, `:1746` | **`:1507` and `:1765`** |
| `SlideAgent._format_slide_context` at `agent.py:885-916`, wrapper at `:908-916` | the class is **`SlideGeneratorAgent`** (`:163`), the method def is **`:904`**, the `<slide-context>` wrapper is **`:928`**. It has **two** call sites: `:1392` and `:1646` |
| `spotlight()`'s docstring "cites review finding #7" | finding #7 is in the **module-level** docstring (`spotlight.py:6`), not the function's (`:29`) |
| `_get_prompt_content` imported at 12 sites | **11**. `_build_tools` **19** is correct. Total private-symbol imports **30**, not 31 |
| `agent.py` has six `agent_factory` comment mentions at `:201,:204,:209,:247,:715,:1843` | **seven**, at `:201,:204,:209,:247,:627,:734,:1862`. `:715` and `:1843` are wrong and `:627`/`:734` are missing from the plan's list. The substantive claim holds: **all seven are comments; `agent.py` imports `agent_factory` nowhere** |
| C8's ten `agent_factory` anchors (`:134,:170,:194,:216,:292,:294-295,:315-350,:359-360,:404-406` and `:141,:206,:229,:335`) | every symbol exists; **every line number is off by 2–20**. Search by name, never by number |
| `test_design_system_templates.py:652` carries the `slide` class token | **`:654`** |

**Two systematic causes, both worth knowing:** `agent.py` and `agent_factory.py` have both grown
since the plan was drafted (a consistent downward shift in the later half of each file), and
ws4b's edits moved `session_manager.py` and `test.yml` again. **Re-grep by symbol name before
every edit.** A stale anchor here is a silent misfile, not an error.

## 21. `chat_service.py` is at `src/api/services/`, not `src/services/`

**[scout, re-probed]** `find src/ -name chat_service.py` → `src/api/services/chat_service.py`.
The plan cites it unqualified nine times. Anything importing or patching
`src.services.chat_service` raises `ImportError`. Same for `session_manager.py`
(`src/api/services/`) and `slide_repository.py` (`src/api/services/`).

## 22. `commit_placeholder` takes NO `modified_by` and NO `deck_spec_slide`

**[measured]** `src/api/services/slide_repository.py:275-331`:

```python
def commit_placeholder(self, session_id: str, position: int, error_message: str = "") -> None:
    ...
    self.write_slide(session_id=..., position=..., html=placeholder_html, scripts="",
                     verification_record={compute_slide_hash(placeholder_html): {...}})
```

C4 says *"Pass `modified_by=initiated_by` ... on every reviewer, fixer **and placeholder** write"*
and also mandates `commit_placeholder` as the placeholder API (*"detect failures via
`is_placeholder_record`, never an HTML class"*). Those two cannot both be satisfied: the method
exposes neither parameter, and `write_slide` with `modified_by=None` leaves the author **NULL on an
INSERT** — which is a placeholder's normal case.

**Ruling C-15: placeholder rows ship with a NULL author and no `deck_spec_slide`, and ws4c does not
widen `commit_placeholder`.** Reasons: ws4c's own scope statement names `agent_factory.py` and the
test files that pin what moved as *the only* existing files it modifies; the plan's own test intent
pins the author/spec assertions to a **reviewer**-written row (*"a reviewer-written row has a
non-NULL author and a parsed `deck_spec_slide`"*), not a placeholder one; and nothing in ws4c, ws4d
or ws4e consumes attribution on a terminal-failure marker.

State it in `placeholder_node`'s docstring so it reads as a decision, not an oversight. If ws4d's
attribution work needs it, two optional pass-through kwargs on `commit_placeholder` is an additive
four-line change in a file that is neither frozen nor barred by R2 — cheap to add then.

Cost if wrong: placeholder rows show no author in ws4d's attribution surface, and ws4d adds the
kwargs.

## 23. `write_slide`'s full signature — the three parameters no plan text names

**[measured]** `src/api/services/slide_repository.py:84-92`:

```python
def write_slide(self, session_id: str, position: int, html: str, scripts: str = "",
                verification_record: Optional[Dict[str, Any]] = None,
                deck_spec_slide: Optional[Dict[str, Any]] = None,
                modified_by: Optional[str] = None,
                slide_id: Optional[str] = None) -> None
```

`position`, `html` and `scripts` are named nowhere in C4 and every call must supply the first two.
Two further facts from its docstring, both load-bearing:

- **`verification_record` MUST be `{content_hash: verdict}`** — the shape `get_slide_deck`'s row
  branch reads back with `.get(compute_slide_hash(row.html))`. A record keyed any other way is
  invisible to the UI **and** pollutes `get_verification_map`'s flat aggregate, which is persisted
  into save points. Build it with `build_verification_record(...)` (§7), never by hand.
- **`deck_spec_slide` takes a `dict`**, serialised to JSON on write. Pass
  `slide_spec.model_dump()`, not the pydantic object.
- On an UPDATE with `slide_id=None` the existing `slide_id` is preserved; on an INSERT a fresh
  uuid4 is generated. Reviewer writes therefore pass `slide_id=None`.

## 24. `write_deck_level_columns` — the signature, and it CONFIRMS C4's five/three split

**[measured]** `src/api/services/deck_level_writer.py`:

```python
def write_deck_level_columns(session_id: str, *, title=_UNSET, css=_UNSET,
                             external_scripts=_UNSET, head_meta=_UNSET,
                             scripts_content=_UNSET, deck_spec=_UNSET,
                             slide_count=_UNSET, html_content=_UNSET,
                             modified_by: Optional[str] = None,
                             expected_version: Optional[int] = None) -> Dict[str, Any]
```

**Every column parameter is keyword-only and defaults to `_UNSET`**, which means "leave the stored
value alone". An explicit `None` means "write SQL NULL" — different operations, and this is why
there are no `None` defaults.

The module's own docstring table settles Ruling C-12 independently of the plan:

    title                  pre-fan-out
    css                    BOTH
    external_scripts_json  pre-fan-out
    head_meta_json         pre-fan-out
    deck_spec_json         pre-fan-out
    slide_count            post-commit
    html_content           post-commit
    scripts_content        post-commit

`read_deck_spec(session_id) -> Optional[Dict[str, Any]]` returns `None` for a missing, NULL, empty,
unparseable or non-object column — a logged warning, never an exception.

## 25. `compute_deck_digest` takes a LIST OF HTML STRINGS, in deck order

**[measured]** `src/services/deck_review_store.py:104` —
`compute_deck_digest(slide_htmls: List[str]) -> str`. Not a deck object, not slide rows. Its
docstring's three documented properties, all of which a test may assert:

- **order matters** — a reorder changes the digest, deliberately, because a deck review judges an arc;
- **HTML comments are stripped** before hashing, so two decks differing only by comments share a digest;
- **inter-token whitespace is NOT removed** — `"<div>  a  </div>"` and `"<div>a</div>"` hash
  *differently*. Do not assert they match.

## 26. C5's CI job — clone `integration-slides`, and register the file so ws4a's guard passes

**[scout, re-probed]** `.github/workflows/test.yml`: seven `integration-*` jobs at `:129, :174,
:224, :269, :314, :359, :408`. `integration-slides` spans **`:314-357`** and is the only one with a
Postgres service. The `unit-tests` pytest line is **`:110`** and applies **no `-m` filter**.

Four facts the plan does not carry [scout]:

1. **`test_ci_collects_integration_tests.py` has `DELIBERATE_EXCLUSIONS = {}`**, so **any** new
   `tests/integration/` file not named in some job's `run:` block fails ws4a's A5 guard. C5's new
   files must be named in the new `integration-graph` job's `run:` block.
2. **`integration-general` (`:408-461`) sets `DATABRICKS_HOST: ""` and `DATABRICKS_TOKEN: ""`
   deliberately** and has **no Postgres**. It is not a home for a suite that needs a real
   checkpointer, and a test constructing a real `WorkspaceClient` there retries SCIM calls and
   hangs.
3. `test-summary` lists twelve jobs in `needs:` and **must gain `integration-graph`** or the new
   job gates nothing.
4. The job-name grep `'^  [a-z_-]*:'` **misses `e2e-tests`** (`:554`) because the pattern excludes
   digits. Do not use it to enumerate jobs.

**Ruling C-16: clone `integration-slides` — Postgres service, its `DATABASE_URL`/`ENVIRONMENT` env,
`-m "not live"`, a junit artifact — as a new `integration-graph` job, and add it to `test-summary`'s
`needs:`.** The plan says so and the reasons above confirm it: "turn 2 against a real checkpointer"
needs Postgres, and ws4e's E4 job filters `-k layer4`, which excludes this suite by construction.

## 27. `DEFAULT_RECURSION_LIMIT` is 10007 and `Send.__init__` does accept `timeout=`

**[scout]** Both confirm the plan rather than correcting it, and both have been "corrected" wrongly
before, so they are restated here:

- The installed default recursion limit is **10007**. Setting none is correct; the superseded
  plan's ~50 would have made the graph fail *earlier*.
- **`Send.__init__` DOES accept `timeout`** in the installed langgraph. **That is not a licence to
  use it.** A compiled graph fanning into a **sync** node raises on invoke:
  `ValueError: Node timeouts are only supported for async nodes because sync Python execution
  cannot be safely cancelled in-process.` A scout "corrected" ws4b's plan on the signature and was
  wrong. Stall detection uses state-recorded timestamps, and that is the only option.
- **A compiled-graph test fixture MUST be file-backed SQLite.** `StaticPool` with
  `sqlite:///:memory:` hands two Pregel worker threads one connection and **segfaults the
  interpreter** — reproduced 3 of 3 runs on ws4b. This will bite C5.

## 28. `SlideGeneratorAgent`, not `SlideAgent` — and the export scanner nuance

**[scout, re-probed]** The class is `SlideGeneratorAgent` (`src/services/agent.py:163`). C7 names
`SlideAgent._format_slide_context`; that class does not exist. The method is
`SlideGeneratorAgent._format_slide_context` at `:904`, wrapper text at `:928`, called from `:1392`
and `:1646`.

Two more from C7's area [scout]:

- **`export.py` is a CONSUMER of `scripts`, not just a logger.** `src/api/routes/export.py:86` does
  `deck_scripts = slide_deck.get("scripts", "")` and then validates the IIFE and embeds it. C4's
  claim that `export.py:563` "logs it" understates the consequence of leaving `scripts_content`
  NULL.
- **At export time the scanner sees scripts too.** `export.py:159` passes
  `raw_slide_html + deck_scripts + scripts_html` as one string. At the *generation* gate C7's
  "scans HTML only, never scripts" is correct; the export path is different. Not a bug — a nuance,
  and it does not change C7's rule that `scripts` must never be passed as
  `_run_output_safety_gate`'s second argument (that parameter is a zero-arg callable it invokes).

## 29. `FindingLevel` is a `Literal`, not an Enum

**[scout, re-probed]** `src/domain/finding.py:39` — `FindingLevel = Literal["slide", "deck"]`. It
has no `.value`, no members to access and cannot be iterated. Use the string literals. (Ruling C-6
means ws4c consumes it nowhere anyway.)

## 30. `Slide` and `SlideDeck` live in DIFFERENT modules

**[scout]** `SlideDeck` is `src/domain/slide_deck.py:30`; `Slide` is `src/domain/slide.py:32`. C4
needs both (it builds the domain object from the deck dict with `SlideDeck.from_dict`, then calls
`knit()` and reads `.scripts`). Import from both files. There is **no `from_json`** on `SlideDeck`.

## 31. Template facts C6 needs that the plan states loosely

**[scout]**

- `resolve_template_bytes` does not exist; C6 creates it. The in-repo query pattern to copy is used
  four times in `agent_factory.py` (around `:143-155` and `:321-330`): **lazy-import both
  `get_db_session` and `DesignSystem` inside the function**, then
  `db.query(DesignSystem).filter_by(id=..., is_active=True).first()`. `DesignSystem` lives in
  `src/database/models/design_system.py`.
- **`DesignSystemTemplate` has NO column holding only a `<style>` block.** `token_css` holds CSS
  text with no wrapping tags; `layout_html` holds the full entry HTML, which *may* contain inline
  `<style>` blocks. So C6's third return value comes from `token_css`, and its second (the
  "`<style>` block") must be **extracted from `layout_html` and unwrapped** — the CSS text between
  the tags, never the tags. This matters directly: `aggregate_deck_css`'s contract is CSS text, and
  a `<style>`-wrapped element is dropped **silently**, with no exception and no log.
- `normalize_root_tag_selectors` runs inside `materialize_templates`, both at materialisation and
  on the lazy self-heal pass. A synthetic test row constructed **without** `materialize_templates`
  has **un-normalised** `layout_html` — a footgun for C6's own tests.
- `agent_factory` imports `get_db_session` **inside** each function, so a module-level patch does
  not intercept it. Patch where it is imported from.

## 32. Pre-existing findings this PR reports and does not fix

Carried from ws4b, re-confirmed as still open, and **out of ws4c's scope for the same reasons**:
15 lockfile entries resolving from `registry.npmmirror.com`; the circular import that makes
`import src.database.models` fail as the first import in a fresh interpreter; two config predicates
disagreeing about `design_system_id`; and `test_dependencies_resolve_on_proxy` running a real `pip`
resolve under a `live` marker the `unit-tests` job does not filter (`test.yml:110`, no `-m`).

New, and reported rather than fixed: **`_build_tools`' docstring still says the brand tool is added
"ONLY when `config.design_system_id is not None`"** while the code requires
`_design_system_is_active(...)` too. C8 preserves both halves of the condition and must **not**
regenerate that docstring from the condition; fixing the docstring is a one-line change in a file
C8 already rewrites, so C8 may correct it and name the correction in its commit.

---

## 33. BLOCKING — `_get_prompt_content` returns `"slide_style": None` ALWAYS. C3's delegation and its headline guard test are both broken.

**[measured]** `src/services/agent_factory.py:291-298`:

```python
    return {
        "system_prompt": assembled,
        "slide_editing_instructions": None,
        "deck_prompt": None,
        "slide_style": None,
        "image_guidelines": None,
        "pre_assembled": True,
    }
```

Every part is `None` but the assembled prompt. C3's contract for the producer of `resolved_style` is
*"One line: `_get_prompt_content(config)["slide_style"]`"* — **which returns `None`
unconditionally**, for every config, on both modes.

**And the guard test the plan calls irreplaceable would pass vacuously.** C3 says: *"Add one more
assertion that no other test can replace: the graph and the monolith resolve the same bytes from the
same config, asserted by calling `resolve_slide_style(config)` and
`_get_prompt_content(config)["slide_style"]` and comparing. That is the guard against someone later
'optimising away' the delegation."* Both sides would be `None`. `None == None`. It is a textbook
test that cannot fail, guarding the one thing the plan says most needs guarding.

**The null is a pinned contract, not an oversight.** It predates ws4b (introduced by `7ba4b437`,
prompt decoupling) and a test asserts it:

    tests/unit/test_agent_factory.py:208:    assert result["slide_style"] is None

The only other reader is `src/services/agent.py:642` (`prompts.get("slide_style") or ""`) — the
legacy concatenation branch that ws4b's handover records as dead in production, in a file R2 forbids
touching. So **filling the key in is not available**: it would break a passing test that deliberately
pins the shape.

**The resolved style IS computed** — as a local variable inside `_get_prompt_content`
(`agent_factory.py:133-232`), passed to `build_generation_system_prompt` / `build_editing_system_prompt`
and then discarded. The branch produces four values, not one: `slide_style`, `design_system_active`,
`design_system_compiled` and `template_pinned`.

**The plan's two C3 constraints are therefore jointly unsatisfiable.** It requires (a) delegation via
that dict key, and (b) no second copy of the resolution branch — *"A second copy of that branch is
free to regress independently of the monolith's, silently, with no test comparing the two — and the
tombstone case is a defect that has already shipped once."* Given the measured return shape, one must
break.

**Ruling C-17: break (a), keep (b). C8 EXTRACTS the branch instead of merely moving it.**

Concretely, and this is the one place C8 does more than relocate code:

1. C8 moves `_get_prompt_content` into `src/services/agent_resolution.py` (Ruling C-9) and, in the
   same move, lifts `agent_factory.py:133-232`'s style-resolution branch into a new module-level
   function returning all four values it produces — a small frozen dataclass or a `NamedTuple`
   (`ResolvedStyle(slide_style, design_system_active, design_system_compiled, template_pinned)`).
2. `_get_prompt_content` **calls** it. Behaviour-preserving: the same bytes reach
   `build_*_system_prompt`, and the returned dict is unchanged, so
   `test_agent_factory.py:208` keeps passing.
3. **`resolve_slide_style(config) -> str` is a thin wrapper returning `.slide_style`.** That is C3's
   contract, satisfied with **one** implementation rather than a delegation through a null key.
4. **The guard test becomes structural, and falsifiable.** There is now literally one implementation,
   so "the graph and the monolith resolve the same bytes" holds by construction. Replace the
   `None == None` comparison with a test that pins the *coupling*: patch `resolve_slide_style` to
   return a marker and assert the marker appears in `_get_prompt_content`'s assembled
   `system_prompt`. **Sabotage it** by making `_get_prompt_content` re-inline the branch instead of
   calling the helper, and confirm red — that is exactly the "optimised away the delegation"
   regression the plan wants caught, and it is now catchable.

Why this is not scope creep: C8 is already rewriting this module wholesale, the change is
behaviour-preserving, and it is covered by 374 existing tests across the six suites C8 must run
anyway. The alternative — a second copy of a branch whose tombstone case has already shipped a
defect once — is the outcome C3 spends a paragraph forbidding.

Cost if wrong: a behaviour-preserving refactor inside one module, reversible, and caught immediately
by the six suites if it is not behaviour-preserving.

**Consequence for the task order:** this makes C8-before-C3 (corrections §14) not merely tidier but
required — C3 cannot write `resolve_slide_style` at all until C8 has extracted the branch.

## 34. `agent_factory.py` has only FIVE module-level functions — and the superseded plan's shim list names five symbols that are not module attributes at all

**[measured]** Every module-level `def` in the file's 522 lines:

    39:  def _create_model()
    78:  def _get_prompt_content(config, mode="generate")
    301: def _design_system_is_active(design_system_id)
    339: def _build_tools(...)
    461: def build_agent_for_request(config, session_data, mode="generate")

The superseded plan's shim (quoted in corrections §13) re-exports
`build_selected_template_block`, `build_type_scale_reassertion`, `extract_type_scale_block`,
`strip_type_scale_region_markers` and `get_template_for_generation`. **None of those is a
module-level name of `agent_factory` today** — all five are **function-local imports inside
`_get_prompt_content`** (`:147`, `:182-183`, `:272-273`). Re-exporting them would *add* five names
that do not currently exist, and the superseded plan's own move test
(`hasattr(agent_factory, name)`) would have been asserting a surface it invented.

**Ruling C-18: the shim re-exports exactly what is actually imported or patched from
`agent_factory` today, and nothing else.** The measured surface is in
`.ws4c-MEASURED-INTERFACES.md` under "THE 22 PATCH TARGETS": `build_agent_for_request`,
`_create_model`, `_get_prompt_content`, `_build_tools`, `_design_system_is_active`, plus the six tool
builders and `search_images` that `_build_tools` resolves in its own module namespace
(`build_genie_tool`, `build_vector_tool`, `build_mcp_tools`, `build_model_endpoint_tool`,
`build_agent_bricks_tool`, `build_ds_asset_tool`, `search_images`).

Adding names to a shim is not free: a name re-exported for no caller is dead surface that the next
PR has to decide about.

## 35. `build_agent_for_request` and `_create_model` STAY in `agent_factory`

**[measured]** `build_agent_for_request` (`:461-522`) is agent *construction* — it builds the model,
the tools and the prompts and returns a `SlideGeneratorAgent`. §L6's subject is the **resolution**
logic. So the move set is exactly three functions: `_get_prompt_content`, `_build_tools`,
`_design_system_is_active` (plus §33's extracted style-resolution helper).

`_create_model` stays too, and that has a useful consequence: the ten `patch(...)` sites naming
`src.services.agent_factory._create_model` (5) and
`src.services.agent_factory._get_prompt_content` (5) **keep working unchanged**, because
`build_agent_for_request` stays in `agent_factory` and resolves both names in `agent_factory`'s
namespace — `_get_prompt_content` being re-exported there by the shim.

## 36. BLOCKING FOR C8 — 12 of the 22 patch targets stop intercepting anything, silently

**[measured]** All 22 `patch("src.services.agent_factory.X")` sites live in
`tests/unit/test_agent_factory.py`. The other five suites have **zero** patch targets and only plain
imports, so they need import repointing at most.

| symbol | sites | after the move |
|---|---|---|
| `_get_prompt_content` | 5 | **still works** — `build_agent_for_request` stays and reads it in `agent_factory`'s namespace |
| `_create_model` | 5 | **still works** — stays in `agent_factory` |
| `build_mcp_tools` | 3 | **BREAKS** — `_build_tools` now resolves it in `agent_resolution`'s namespace |
| `build_vector_tool` | 2 | **BREAKS** |
| `build_model_endpoint_tool` | 2 | **BREAKS** |
| `build_ds_asset_tool` | 2 | **BREAKS** |
| `build_agent_bricks_tool` | 2 | **BREAKS** |
| `_design_system_is_active` | 2 | **BREAKS** — called by the moved `_build_tools` |
| `build_genie_tool` | 1 | **BREAKS** |
| `search_images` | 1 | **BREAKS** |

**Why this is worse than a plain failure: `unittest.mock.patch` on a name the code no longer reads
does NOT error.** The patch applies to `agent_factory`'s attribute, the mock is simply never called,
and the test proceeds against the *real* tool builder. A test that then asserts on the mock
(`assert_called_once`) fails loudly — fine. **A test that only asserts on the result passes
vacuously**, and now exercises real tool construction it was written to stub out. That is a
test-that-cannot-fail factory, in the exact class this repo keeps catching.

**Ruling C-19: C8 repoints all 12 to `src.services.agent_resolution.X`, and proves it did so by
consequence rather than by grep.** The proof: before repointing, run
`tests/unit/test_agent_factory.py` and record which tests fail; after repointing, all 33 pass. Then
**sabotage it** — revert ONE repointed target to `src.services.agent_factory.X` and confirm a test
goes red. If reverting a target reddens nothing, that test was passing vacuously **before** this PR
too, and that is a finding to report rather than a thing to leave.

## 37. The 13 `src.services.agent` test files, enumerated — and the overlap C7 and C8 must not collide on

**[measured, `--include="*.py"`; an earlier count of mine was contaminated by `.pyc` files in
`__pycache__`, which is worth knowing because the naive grep silently inflates it]**

    tests/integration/test_slide_replacement_flow.py
    tests/unit/test_add_position_bug.py
    tests/unit/test_agent_factory.py
    tests/unit/test_agent_safety_gate.py
    tests/unit/test_agent.py
    tests/unit/test_default_config_integration.py
    tests/unit/test_error_recovery.py
    tests/unit/test_intent_detectors_log_extra.py
    tests/unit/test_llm_edit_responses.py
    tests/unit/test_safety_gate_http.py
    tests/unit/test_slide_context_injection.py
    tests/unit/test_slide_editing_robustness.py
    tests/unit/test_slide_replacements.py

Thirteen, confirming the plan's count. **None moves** — `agent.py` is unmodified, so there is nothing
to repoint and no new home to name, and all 13 must still collect and pass where they are.

Two overlaps, both benign but worth stating so two tasks do not edit one file:

- **`test_agent_factory.py` is in BOTH sets** — it is one of the 13 *and* one of C8's six suites
  (13 `src.services.agent` refs, 47 `agent_factory` refs). **C8 owns it.** C7 must not touch it.
- **Three of the 13 are C7's security suites** — `test_agent_safety_gate.py`,
  `test_safety_gate_http.py`, `test_slide_context_injection.py`. **C7 owns those three**, adding
  graph-path cases alongside their existing monolith cases. C8 must not touch them.

The two file sets are otherwise disjoint. Ruling R1's deletion rule does not apply anywhere here:
the functionality survives.

## 38. `ensure_deck_token_css` is in `design_system_templates.py`

**[measured]** `src/services/design_system_templates.py:381` —
`ensure_deck_token_css(deck_css: Optional[str], token_css: Optional[str]) -> str`. ws4b's corrections
say only that it is not in `css_utils.py`; it is not in `design_system_compiler.py` either.

## 39. A second committed artifact: `.ws4c-MEASURED-INTERFACES.md`

Because sixteen of the plan's anchors are wrong and two more of its claims about signatures turned
out to be wrong in ways that would have shipped defects (§33, §34), the interfaces every task binds
to are extracted verbatim into
**`docs/superpowers/plans/.ws4c-MEASURED-INTERFACES.md`** (~2,750 lines, 80 sections).

**It is generated by script — `scripts/gen_ws4c_measured_interfaces.sh` — not typed**, so no block in
it can have drifted in transcription. Every task brief points at it instead of at the plan's
anchors. Regenerate it if a task edits one of the files it quotes; the line numbers are correct as of
the commit that adds it and are hints, not addresses.

---

## 40. CONTROLLER'S OWN CORRECTIONS — four errors in this file, found by C8 and re-probed

C8 verified this file against the code rather than transcribing it, and found four errors in my own
corrections. **All four re-probed by the controller; all four confirmed.** They are recorded here
because §33 and §36 are what C3 and any later reader bind to.

### 40.1 §33 undercounts the branch's outputs — it produces FIVE, not four

I wrote that the style-resolution branch produces `slide_style`, `design_system_active`,
`design_system_compiled` and `template_pinned`. **The legacy limb also sets `image_guidelines`**
[measured — `agent_factory.py` pre-move, the `elif config.slide_style_id is not None` limb:
`image_guidelines = style.image_guidelines`].

**How it was caught is the point:** C8 implemented my literal four-value version **as a sabotage** and
it reddened four tests, two of them pre-existing. A faithful reading of my correction would have
**dropped legacy image guidance out of every prompt** — a silent regression on the monolith path,
introduced by the correction meant to prevent silent regressions.

`ResolvedStyle` therefore carries **five** fields. **Ruling C-17 stands; its output list was wrong.**

### 40.2 §33's coupling-test recipe is unsatisfiable as written

I specified: patch `resolve_slide_style` to return a marker and assert the marker reaches
`_get_prompt_content`'s assembled `system_prompt`. **That cannot work** — `resolve_slide_style` is a
thin wrapper *over* the extracted resolver, so patching it does not intercept
`_get_prompt_content`'s own call without resolving twice: two DB round trips and a doubled lazy
recompile-and-commit.

C8 replaced it with two tests patching the **real seam** (the extracted resolver), both
sabotage-verified. **The intent of the ruling holds — the coupling must be pinned by a test that can
fail — and the mechanism is C8's, not mine.** The re-inlining sabotage leaves **465 of 466** tests
passing, so that guard is the only thing standing between this and a silent regression.

### 40.3 §36's patch-target inventory is wrong: 25 targets in THREE files, and 15 break

I wrote "22 targets, all in `test_agent_factory.py`; 12 break" and "the other five suites have zero
patch targets". **All three claims are wrong** [measured].

The cause is worth recording, because it will recur: my per-file count used
`grep -c 'patch("src\.services\.agent_factory\.'`, which **cannot match a multi-line call**. Both
missed targets are written as

    with patch(
        "src.services.agent_factory._design_system_is_active", return_value=True
    ):

so the `patch(` and the string are on different lines. Corrected:

| | value |
|---|---|
| total targets | **25**, across `test_agent_factory.py` (23), `test_ds_generation_state_matrix.py` (1), `test_factory_tool_spotlighting.py` (1) |
| break on the move | **15** — the six tool builders, `search_images`, and `_design_system_is_active`, all resolved by `_build_tools` in its own namespace |
| keep working | **10** — `_get_prompt_content` (5) and `_create_model` (5), because `build_agent_for_request` stays in `agent_factory` |

Nothing was blocked: all three files were C8's to edit. **But had one been assigned to a sibling
agent, that would have been a file collision my own inventory concealed.** When counting call sites,
grep the dotted string, never the `patch(` prefix.

### 40.4 The six-suite harness collects 444, not 374

I wrote 374 from the scouts' `grep -c "def test_"`. **That counts test FUNCTIONS, not test CASES** —
parametrised tests collect many cases per function. Measured with `--collect-only`:

    test_ds_generation_state_matrix   16
    test_design_system_compiler      282     (I said 215)
    test_prompt_precedence_fixes      19
    test_factory_tool_spotlighting     3
    test_agent_factory                33
    test_design_systems_routes        91     (I said 88)
    ----                             444

**Never quote a test count taken from a `def test_` grep.** Use `pytest --collect-only -q`.

## 41. Ruling C-20 — the shim does NOT re-export the tool builders. This NARROWS Ruling C-18, deliberately.

C8 asked for a second opinion on the one place it departed from my instruction, which is exactly the
right thing to escalate. **Ruling C-20: the narrowing is ACCEPTED, and it is better than C-18.**

C-18 said the shim re-exports the measured import surface, which included the six tool builders and
`search_images`. C8 left them off. The consequence, and the reason:

- **A stale patch target now raises `AttributeError` instead of silently intercepting nothing.** That
  is precisely the hazard §36 exists to warn about — `patch()` on a name the code no longer reads
  does not error, so a test asserting only on the result passes vacuously against real tool
  construction. C8's narrowing converts that silent vacuous pass into a hard failure, **and it is why
  all 15 broken targets failed loudly rather than quietly.**
- **Nothing loses a working import.** The only production importer of `agent_factory` is
  `chat_service.py:32`, which imports `build_agent_for_request` — retained. Every other consumer was
  a patch target, now repointed. `_build_tools` resolves the builders in `agent_resolution`'s
  namespace, where they live.

Cost if wrong: an unmeasured consumer patching one of those names breaks loudly rather than silently.
**That is the direction we want to fail in**, and the consumer set was measured.

C-18 is amended: **the shim re-exports `build_agent_for_request`, `_create_model`,
`_get_prompt_content`, `_build_tools` and `_design_system_is_active` — and nothing else.**

## 42. Reported, not fixed — a pre-existing weak assertion C8 surfaced

`test_factory_image_tool_wraps_output` asserts only on the spotlight wrapper, so an **ineffective
stub is invisible** wherever the real `search_images` happens to succeed. Repointing restores its
stub, so the test works again; the weak assertion is pre-existing and is **reported rather than
fixed**, in line with how ws4a and ws4b handled pre-existing findings outside their scope.

## 43. A note for ws4d, measured by C8 and worth carrying forward

**`build_agent_for_request`'s presence on `agent_factory` gates collection of the ENTIRE unit
suite**, via `tests/unit/conftest.py` → `session_manager` → `src/api/services/__init__.py` →
`chat_service`. ws4d rewrites `chat_service`; if it removes that import without also removing the
chain, the whole unit suite stops collecting rather than failing a test.

## 44. §35 corrected again — "ten targets keep working" rests on the SHIM, not only on what stays

§35 explains that the ten `_get_prompt_content` and `_create_model` patch targets survive the move
"because `build_agent_for_request` stays in `agent_factory` and resolves both names in
`agent_factory`'s namespace". Incomplete: **`agent_factory.py:128` also reads `_build_tools`**, which
is a *re-exported* name, so the surviving targets depend on the shim's re-export as much as on which
functions stayed [found by the C8 reviewer, confirmed]. Consequence: removing a name from the shim
does not merely break a patch target, it breaks `build_agent_for_request` itself — which is why
dropping `_build_tools` from the shim reddens 28 tests across the harness rather than a handful.

## 45. A guard can be blind to HALF a branch — and the half it misses is the one with the defects

Recorded because it is the sharpest finding of the build so far, and it generalises well beyond C8.

C8's coupling guard — the single test standing between the extracted `resolve_style_source` and a
silent re-inlining — called `_get_prompt_content(AgentConfig())`. An `AgentConfig` with no
`design_system_id` and no `slide_style_id` **only ever exercises the non-design-system path.** The
reviewer routed the *entire design-system limb* through a byte-identical second copy of the branch and
measured **466 of 466 tests passing: nothing went red.** The `resolve_slide_style` wrapper guard had
the identical blind spot.

So the guard protected the two-line legacy `slide_style_id` lookup and was blind to the limb carrying
**the tombstone case, the sentinel-bearing `design_system_compiled` copy, the lazy
`compiled_style_content` recompile, and the pinned-template block** — every shipped defect §L6 exists
to preserve. And its docstring asserted the coverage it did not have.

**The lesson, stated generally: a sabotage that reddens a test proves the test covers the path the
sabotage was on, and nothing more.** C8 sabotaged the branch and saw red, correctly, and reasonably
concluded the guard worked. It did — for one config. **When a function branches, the falsification
must be run once per limb, or parametrised over inputs that reach each limb.** "I broke it and a test
failed" is not evidence about the limbs the test never entered.

The fix is a two-line parametrisation over `AgentConfig()` and `AgentConfig(design_system_id=n)`,
verified against both the shipped code and the probe before being proposed.

## 46. MEASURED — a NamedTuple in `GraphState` is on a deprecation path. State carries plain scalars only.

C3 gave `call_skill` and `assemble_skill_prompt` a third parameter typed `ResolvedStyle` (C8's
five-field NamedTuple). Its reasoning was sound — the frame-constraint and DS-precedence conditionals
cannot be decided without style information at call time, and `GraphState.resolved_style` is declared
`Optional[str]`, which carries the text but not the active flag. But the type is wrong, and two probes
settle it.

**Probe 1 — a NamedTuple in `GraphState`, read back on turn 2 through the real
`SqlAlchemyCheckpointSaver` (file-backed SQLite):** [measured]

    TURN 1: type=RS  value=RS(slide_style='STYLE', design_system_active=True)   .design_system_active -> True
    TURN 2: type=RS  value=RS(slide_style='STYLE', design_system_active=True)   .design_system_active -> True

It round-trips **today** — and langgraph says it will not for long:

    Deserializing unregistered type __main__.RS from checkpoint. This will be blocked in a future
    version. Set LANGGRAPH_STRICT_MSGPACK=true to block now, or add to allowed_msgpack_modules to
    allow explicitly

So a `ResolvedStyle` in checkpointed state is a latent break: a langgraph upgrade — and this repo
upgrades langgraph, that was PR2 — or anyone setting that env var turns turn 2 into a hard failure.

**Probe 2 — a NamedTuple inside a `Send` PAYLOAD, consumed in the same superstep:** [measured]

    branch saw type=RS  .design_system_active=True
    msgpack/unregistered warnings: NONE

No warning, because the payload never round-trips before the branch reads it. **So the object is safe
as a function parameter and unsafe as state**, and the distinction is exactly "does it cross a
checkpoint boundary".

**And the parameter does not need to be an object at all.** `assemble_skill_prompt` reads **exactly
one field** — `design_system_active`, at two lines. The style *prose* reaches the skill through
`json.dumps(payload)`, which is what the plan intended when it had `build_branch_payload` copy
`resolved_style` into the payload.

**Ruling C-21:**

- `assemble_skill_prompt(skill, payload, design_system_active: bool)` and
  `call_skill(name, payload, design_system_active: bool)`. A `bool`, not a `ResolvedStyle` — a
  parameter typed as a five-field object of which one field is read would force every call site to
  build three dummy values it does not have.
- **`GraphState` gains `design_system_active: Optional[bool]`** — single-writer, written by
  `architect_node` from `resolve_style_source(...).design_system_active`, no reducer.
  `resolved_style` stays `Optional[str]` as C1 declared it.
- **`build_branch_payload` copies both plain scalars.** Nothing but JSON-native types enters
  `GraphState` or a `Send` payload anywhere in ws4c.

Why the three-case table collapses to one boolean, which is worth stating so it does not look like a
shortcut: case 1 (design system active) is the only case whose style already carries the safe-area
numbers, and cases 2 and 3 both lack them and both have `design_system_active == False`. The plan's
test intent asks for exactly this — *"frame constraints injected in cases 2 and 3 and not duplicated in
case 1"* — so one flag satisfies it. A content sniff of the style text would be more robust against a
library style that happens to contain the numbers, but the plan does not ask for that and a false
negative there costs a duplicated block, not a wrong prompt.

Cost if wrong: one parameter and one declared key, both changed in a single place.

## 47. Ruling C-22 — C9 splits the seven skills into modules; C3 put them inline

C3 registered all seven skills **inline in `src/core/skills/__init__.py`** (245 lines). The plan's C9
text says "Modify: the seven skill **modules** under `src/core/skills/`", so those modules do not
exist and C9 creates them: one per skill, with `__init__.py` retaining the loader, the frozen `Skill`
dataclass, `load_skill`, `list_skills`, `call_skill` and the registry.

Reasons: the real prose is long (it is copied from five `prompt_modules` constants) and would bury a
loader that is the only part with logic in it; the plan's own wording is the tie-breaker; and it
shrinks C9's edit surface on `__init__.py`, the one file in that package another task imports.

**Sequencing requirement, stated because a combined change is unattributable:** move each body
**verbatim** first and confirm C3's `tests/unit/test_skills.py` still passes, and only then rewrite
the prose. Those tests pin the registry, the schema bindings, the non-empty bodies and the grants, so
they are the safety net for the move — but only if the move is a separate step.

Cost if wrong: seven files become one again, mechanically.
