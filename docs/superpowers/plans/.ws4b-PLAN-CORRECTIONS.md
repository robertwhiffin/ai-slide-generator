# ws4b — plan-vs-code corrections pre-pass

**Written 2026-09-11, before Task B1.1.** Branch `feat/ws4b-contracts-and-schema`, off
`feat/langgraph-core` @ `87f22c5a` (ws4a merged at `a2dcae9e`).

**THIS FILE OVERRIDES THE PLAN** (`docs/superpowers/plans/2026-08-25-ws4b-contracts-and-schema.md`)
wherever the two disagree. Every task dispatch is pointed at it.

Provenance is marked on every item:

- **[measured]** — the controller ran the command and read the output.
- **[scout]** — a read-only scout reported it and quoted evidence; not independently re-run.
- **[scout, re-probed]** — a scout reported it and the controller re-ran the check.

Bulk evidence (full quotes, per-claim commands) lives in the gitignored session workspace
`.superpowers/sdd/2026-08-25-ws4b-contracts-and-schema/scout{1,2,3,4}-*.md`. Everything
load-bearing has been copied into this file, because that workspace is deleted when the plan
completes.

---

## 0. Environment facts measured this session

| Fact | Value |
|---|---|
| Interpreter | `~/.pyenv/versions/3.11.0/bin/python` — shared pyenv site-packages. **Never `pip install`.** |
| langgraph | **1.2.10** [measured] |
| langgraph-checkpoint | **4.1.1** [measured] |
| Local PostgreSQL | **REACHABLE**: `PostgreSQL 14.20 (Homebrew) on aarch64-apple-darwin25.1.0` at `postgresql+psycopg2://localhost:5432/postgres` [measured] |
| npm registry | `https://npm-proxy.cloud.databricks.com/` (the Databricks proxy) [scout] |
| `frontend/package-lock.json` today | 7538 lines, **0** `artifactory` hits, 520 `registry.npmjs.org` hits [scout] |

### 0.1 NEW HAZARD, not in the plan or the handover: `.claude/worktrees/`

`.claude/worktrees/` holds **9 stale agent worktrees**, each a full copy of the repo tree. A
naive `grep -rn "<symbol>" .` returns roughly **ten times** the real hit count, every duplicate
looking like a genuine second call site. [measured — a `ConfigPrompts(` sweep returned ~120 hits,
of which 11 were real.]

**Every repo-wide grep must exclude them.** The working form:

    grep -rn "<pattern>" . | grep -vE "node_modules|/\.venv/|/build/|\.egg-info|/dist/|__pycache__|\.claude/worktrees"

Prefer scoping to `src/ tests/ frontend/src/ scripts/ .github/` instead of `.`.

### 0.2 The `.env` contamination rule still binds

`src/core/database.py` calls `load_dotenv()`. The local `.env` carries `DATABASE_URL`
(**postgresql**, i.e. the local Homebrew instance — *not* sqlite), `DATABRICKS_HOST`,
`DATABRICKS_TOKEN`, `ENVIRONMENT`, and three `DEV_USER_*` keys. [measured — key names only.]
An explicit env var overrides it. Measure anything touching a Databricks client with the CI env.

### 0.3 There is a `postgres` pytest marker and a self-skip precedent — use it

`pyproject.toml:106` declares `postgres: marks tests that require a reachable PostgreSQL (they
self-skip when none is)`. The in-repo pattern to copy is
`tests/unit/test_design_system_partial_name_index_postgres.py`: read
`os.environ.get("TELLR_TEST_POSTGRES_URL", "postgresql+psycopg2://localhost:5432/postgres")`,
attempt `SELECT 1`, and `pytest.skip(..., allow_module_level=True)` when unreachable. [measured]

This is the mechanism for every Postgres-only proof in ws4b (`ALTER TABLE … DROP COLUMN`, the
partial index on `spec_dirty_at IS NOT NULL`), and for the checkpointer's live test.

### 0.4 Lakebase — status, and what it means for the Definition of Done

`databricks database get-database-instance db-tellr -p tellr-dev` returns **`Error: Resource not
found`**; `list-database-instances` on that profile returns exactly one instance, `db-tellr-TY`.
[measured] The operator states the production Lakebase **is** called `db-tellr`, so the 404 is a
visibility/permission result rather than proof of absence — do not conclude the instance is gone.

`config/deployment.yaml` pins both `production` and `devloop` to `LAKEBASE_INSTANCE: db-tellr`,
and `devloop` sets `branch_from: production`. An app `db-tellr-dev-agent-7277` is
**ACTIVE / SUCCEEDED** on that workspace. [measured]

**Operator ruling (recorded, not inferred): the publish-and-deploy route is authorised** — a
`.devN` to real PyPI plus a `devloop` instance, torn down afterwards. Revisit at B2.5; do not
attempt it before the four migrations exist.

---

## 1. THE BASELINE — re-derived on this branch before any edit

Command: `~/.pyenv/versions/3.11.0/bin/python -m pytest tests/ -n auto -q`, no app server
running. [measured 2026-09-11]

    3 failed, 4212 passed, 8 skipped, 332 warnings in 178.67s
    COLLECTED: 4223   (pytest tests/ --collect-only -q)

**Identical to `docs/superpowers/baselines/pr3_ws4_collected.log` Section 2 — same three tests,
same four assertion strings.** The pre-change baseline therefore needs no correction; ws4b must
still UPDATE Section 2 with its own post-change measurement, because it changes schemas, the ORM
and dependencies.

CAUSE 1 — deploy-autoscaling (reproduces anywhere). **Two distinct assertion strings; never grep
for only one:**

    tests/unit/test_deploy_autoscaling.py::TestGetOrCreateLakebase::test_returns_autoscaling_when_available
      E  AssertionError: assert 'provisioned' == 'autoscaling'
    tests/unit/test_deploy_autoscaling.py::TestGetOrCreateLakebase::test_falls_back_when_autoscaling_creation_fails
      E  AssertionError: Expected '_get_or_create_lakebase_provisioned' to have been called once. Called 0 times.

CAUSE 2 — stale stored Genie space id (ENVIRONMENTAL, local `.env` dependent):

    tests/integration/test_genie_integration.py::test_genie_conversation_continuation
      E  databricks.sdk.errors.platform.NotFound: Space with id 01effebcc2781b6bbb749077a55d31e3 not found
      E  src.services.tools.genie_tool.GenieToolError: Failed to query Genie space after 3 attempts: ...

Clause 3 of the gate ("no test stopped existing") **cannot be checked by a count.** Diff the test
trees for removed `def test_` / `async def test_` / `class Test` lines and for deleted files.

---

## 2. A FALSE CORRECTION, REJECTED — do not act on it

A scout reported that the plan's claim "`Send(timeout=)` is unusable" is WRONG, on the grounds
that `Send.__init__` in langgraph 1.2.10 accepts `timeout: float | timedelta | TimeoutPolicy |
None = None` as a keyword-only argument.

**The scout inspected the signature; the plan makes a runtime claim.** Both are true, and the
plan is the one that matters. Re-probed by the controller — a compiled graph fanning out with
`Send("worker", {...}, timeout=5.0)` into a **sync** node raises on `invoke`:

    ValueError: Node timeouts are only supported for async nodes because sync Python execution
    cannot be safely cancelled in-process. Node 'worker' is sync.

**B2.1's module docstring keeps the claim as written.** The precise wording to use: the parameter
exists on `Send` but is rejected at run time for sync nodes, and Tellr's generation path is sync
end to end.

---

## 3. Corrections that make the work SMALLER

### S1 — every deck-level column B3.1 writes ALREADY EXISTS. B3.1 adds no columns.

`src/database/models/session.py:236` `class SessionSlideDeck` already declares, verbatim
[measured]:

    title = Column(String(255))
    html_content = Column(Text)
    scripts_content = Column(Text)
    slide_count = Column(Integer, default=0)
    deck_json = Column(Text)
    verification_map = Column(Text, nullable=True)
    deck_spec_json = Column(Text, nullable=True)
    css = Column(Text, nullable=True)
    external_scripts_json = Column(Text, nullable=True)
    head_meta_json = Column(Text, nullable=True)
    locked_by / locked_at
    version = Column(Integer, default=0, nullable=False)     # the optimistic lock B3.1 uses
    modified_by = Column(String(255), nullable=True)

So **B3.1 is a writer only** — no migration, no `ADD COLUMN`. `expected_version` has a real
column behind it.

### S2 — the three genuinely-new schema objects, confirmed absent

`grep -rn "spec_dirty\|deck_reviews\|graph_checkpoints\|graph_checkpoint_writes" src/ tests/`
returns **nothing** [measured]. B2.1, B2.2 and B2.3 each create something that does not exist.
`src/domain/` holds only `slide.py` and `slide_deck.py` — `finding.py`, `deck_spec.py` and
`skill_io.py` are all new [scout]. `src/core/checkpointer.py`,
`src/api/services/deck_level_writer.py`, `src/services/deck_css_aggregator.py` and
`src/services/deck_review_store.py` are all absent [scout].

### S3 — one stated justification is false; the test still earns its place

B1.2 says `VerificationBadge.tsx` "dereferences `.rating` and `.issues.length` (lines 119, 201)
**without null-checking the whole value**". It does null-check —
`frontend/src/components/SlidePanel/VerificationBadge.tsx:117` is `if (!verificationResult)
return null;`, immediately above the `:119` dereference [measured].

**Keep the `VerificationResult` conformance test** — the Python/TS drift it guards is real and
has already happened. **Do not write an assertion premised on a missing null-check**, and do not
"fix" `VerificationBadge.tsx`: it is not broken, and touching it is out of scope.

---

## 4. Wrong file paths — nine of them. Use the right-hand column.

Every path in this table appears in the plan's B2.4/B2.5 site lists. All corrected paths verified
with `find src -name <file>` [measured].

| Plan says | Reality |
|---|---|
| `src/api/services/profile_service.py` | **`src/services/profile_service.py`** |
| `src/api/services/settings_db.py` | **`src/core/settings_db.py`** |
| `src/api/services/config_service.py` | **`src/services/config_service.py`** |
| `src/api/schemas/defaults.py` | **`src/core/defaults.py`** |
| `src/api/schemas/config_loader.py` | **`src/core/config_loader.py`** |
| `src/api/schemas/validator.py` | **`src/services/validator.py`** |
| `src/core/agent_factory.py` | **`src/services/agent_factory.py`** |
| `src/core/agent.py` | **`src/services/agent.py`** |
| `src/core/types.py` | **`src/database/types.py`** |

And two schema modules are one directory deeper than the plan says [scout]:

| Plan says | Reality |
|---|---|
| `src/api/schemas/requests.py:35-36, :133-134, :136-141` | **`src/api/schemas/settings/requests.py`** (`PromptsConfigUpdate` at `:128`) |
| `src/api/schemas/responses.py:53-54` (`PromptsConfig`) | **`src/api/schemas/settings/responses.py:44`** |

`src/api/schemas/agent_config.py` **is** at the path the plan gives [scout].

**Ruling R2 still binds on `src/services/agent.py`.** It appears in this table only because
B2.4's site list names it; B2.4's edit there is the retired-prompt-column removal the plan
explicitly scopes, and the handover records `agent.py` as unmodified by all five plans in the
*graph-accommodation* sense. Treat `src/services/agent.py` as **edit-only-where-B2.4-names-it**
and nowhere else — no refactor, no dead-code deletion beyond what B2.4 states.

---

## 5. Wrong anchors

`src/api/services/session_manager.py` is **2912 lines**. The handover's measured shift table
(`+0` at or above `:967`, `+6` at the `SessionSlideDeck(` constructor, uniform `+7` below
`:1014`) **held for every anchor ws4b cites** [scout, spot-checked by controller]:

| Plan anchor | Real anchor | +7 held? |
|---|---|---|
| `:709` `_get_deck_owner_session` | `:709` | yes (`+0` zone) |
| `:1309-1314` optimistic-lock check | `:1317-1321` | yes |
| `:1321` version bump | `:1328` | yes |
| `:1366` `deck.css` assignment | `:1373` | yes |
| `:1403` `_prune_slide_rows_beyond` | `:1410` | yes |
| `:1538-1564` row-read `deck_dict` | `:1544-1562` | yes |
| `:1549` `"scripts": deck.scripts_content or ""` | `:1556` | yes |
| `:1585` normal-path branch | `:1592` | yes |
| `:1637-1650` legacy fallback | `:1644-1655` | yes |
| `:1793-1811` `get_verification_map` | `:1800` (body to ~`:1866`) | yes |
| `:1859` `VERSION_LIMIT` | `:1866` | yes |
| `:1939-1951` `deck_spec_json` snapshot | `:1946-1958` | yes |
| `:2240` `restore_version` copy-back | `:2247` | yes |

**Re-grep by content anyway.** These will move again as ws4b edits the file.

### A1 — `deck.deck_json = None` at `:1303` / `:1320` does not exist

There is **no** two-step null-then-set pattern. `save_slide_deck` computes and assigns
[measured]:

    1310:            deck_json = json.dumps(deck_dict) if deck_dict else None
    1327:                deck.deck_json = deck_json

**The plan's BEHAVIOURAL claim is correct** — `deck_dict=None` does result in `deck_json` being
set to `None`, which is exactly why B3.1 cannot delegate to `save_slide_deck`. Only the anchors
are wrong. An implementer searching for the literal `deck.deck_json = None` will find nothing and
must not conclude the hazard is imaginary.

Other `deck_json` assignments in that file, for orientation: `:983`, `:991` (`duplicate_session`),
`:1630`, `:2237` (`restore_version`).

### A2 — `get_verification_map` is 66 lines, not 18

Body runs `:1800`–`~:1866`, and includes a legacy fallback branch the plan's 18-line range hides
[scout]. B1.1's constraint-3 test must be written against the whole function.

### A3 — `update_css` is at `src/domain/slide_deck.py:97`, not `:108` [scout]

### A4 — `ensure_deck_token_css` is NOT in `css_utils.py`

    src/services/design_system_templates.py:381:
    def ensure_deck_token_css(deck_css: Optional[str], token_css: Optional[str]) -> str:

[measured]. B3.3 imports `merge_css` from `src/utils/css_utils.py` and
`ensure_deck_token_css` **from `src/services/design_system_templates.py`**. It PREPENDS, and it
prepends a comment marker of the form
`/* design-system token stylesheet (re-emitted at save: ...) */` [scout] — which is exactly why
B3.3's idempotence test must assert the semantic invariant, never byte equality.

---

## 6. CI corrections — `.github/workflows/test.yml`

The file is **897 lines** [scout]. Full job map:

| Job | Lines | needs | timeout-minutes |
|---|---|---|---|
| `changes` (paths filters) | 17–87 | — | none |
| `unit-tests` | 89–127 | changes | none |
| `integration-api-routes` | 129–171 | changes, unit-tests | none |
| `integration-config-api` | 174–221 | changes, unit-tests | none |
| `integration-export` | 223–266 | changes, unit-tests | none |
| `integration-streaming` | 268–311 | changes, unit-tests | none |
| `integration-slides` | 313–356 | changes, unit-tests | none |
| `integration-genie` | 358–404 | changes, unit-tests | none |
| `integration-general` | 406–458 | changes, unit-tests | **15** |
| `frontend-build` | 460–487 | changes | none |
| `wheel-build` | 489–525 | changes | none |
| `e2e-tests` (matrix 537–577) | 527–840 | changes, unit-tests, frontend-build | none |
| `test-summary` | 842–897 | all 11 | none |

### C1 — the ConfigPrompts seed step is at `:664`, not `:589`

    .github/workflows/test.yml:664:                  prompts = ConfigPrompts(

[measured]. Line 589 is `--health-cmd pg_isready` (a postgres service option). The seed step's
heading is around `:623` [scout]. **The plan's `:589` is off by ~75 lines.**

### C2 — insert `frontend-unit-tests` between `frontend-build` and `wheel-build`

i.e. after `:487`, before `:489`. That region is disjoint from the paths filters (17–87), every
integration job (129–458) and the e2e matrix (537–577). **Re-read the file; never resolve by hunk.**

### C3 — `test-summary` must gain the new job or it gates nothing

`test-summary` (842–897) enumerates all 11 jobs in its `needs:`. A `frontend-unit-tests` job
absent from that list runs but blocks nothing. [scout] **The plan does not mention this.**

### C4 — the seven production `ConfigPrompts(...)` insert sites, corrected

Verified by a worktree-excluded repo sweep [measured]:

    scripts/run_e2e_local.sh:160
    scripts/init_database.py:218
    .github/workflows/test.yml:664          <- plan said 589
    src/core/init_default_profile.py:408
    src/services/profile_service.py:204     <- plan said src/api/services/
    src/services/profile_service.py:409     <- plan said src/api/services/
    src/services/profile_service.py:490     <- plan said src/api/services/  (clone_profile)

`src/database/models/prompts.py:10` is the class definition and `:51` the `__repr__` f-string —
neither is an insert site. **Count and shape confirmed: seven, two of them inline Python.**

**Plus FOUR test-file constructors** the plan's site list does not enumerate (its R1 triage
covers them by file, not by call site) [measured]:

    tests/unit/test_unset_agent_config_is_sql_null.py:126
    tests/unit/config/test_models.py:87
    tests/unit/config/test_models.py:137
    tests/unit/test_settings_db.py:69

### C5 — `init_database` has FIVE steps, not three

`packages/databricks-tellr-app/databricks_tellr_app/run.py::init_database` order is
`init_db` → `migrate_profiles` + `backfill_sessions` → `backfill_unmigrated_decks` →
`seed_defaults` → `ensure_encryption_key` [scout]. The plan names only three and says the new
strip step goes "after `migrate_profiles`/`backfill_sessions`, before `seed_defaults()`".

**Correction: place `strip_retired_prompt_keys` LAST, after `ensure_encryption_key`.** The plan's
stated *intent* — the preventive edit (B2.4, stop emitting the keys) precedes the data fix
(B2.5, strip existing keys) — is satisfied by any position after `migrate_profiles`, and last is
the position that cannot be invalidated by a later step re-introducing a blob. Each existing step
sits in its own `try` that ends `raise SystemExit(1)`; the new one matches. [Ruling: controller.]

---

## 7. Frontend corrections

### F1 — `frontend/src/components/ui/` does not exist at all

The plan says the `./src/components/ui` alias target is "empty". It is **absent** [scout,
re-probed]. The real location is `frontend/src/ui/` (non-empty: `button.tsx`, `sidebar.tsx`, …).
`@/ui` must resolve to `frontend/src/ui/`. Round-3 finding 27 stands, with a sharper reason: the
alias does not point at an empty directory, it points at nothing.

### F2 — vitest is absent from BOTH `package.json` and `node_modules`

`vitest`, `@testing-library/react`, `@testing-library/jest-dom` and `jsdom` are all uninstalled
[scout]. This is ws4b's **one** permitted dependency addition. The lockfile rule is therefore
live, not theoretical: local npm resolves through `https://npm-proxy.cloud.databricks.com/`, so
after `npm install` the new `resolved` URLs will carry proxy hosts. **Rewrite them to
`registry.npmjs.org` and prove `0` artifactory/proxy hits before committing** — the file is at
0 today, so the target is unambiguous.

### F3 — `playwright.config.ts` has `testDir: './tests'`

It covers the whole `frontend/tests/` tree [scout]. The plan's chosen vitest `include` of
`src/**/*.test.{ts,tsx}` avoids the collision by construction. **Do not put vitest files under
`frontend/tests/`**, and do not add a `testIgnore` — keeping vitest inside `src/` is the plan's
decision and it is the correct one.

### F4 — no tsconfig covers `frontend/tests/`

`tsconfig.app.json` includes only `src`; `tsconfig.node.json` includes only `vite.config.ts` and
`playwright.config.ts` [scout]. So `npm run typecheck` cannot see a Playwright spec at all —
this is the ws4a trap one level down. It also means the plan's tsconfig decision (add
`"vitest/globals"` and `"@testing-library/jest-dom"` to `tsconfig.app.json`'s `types`, and
`vitest.config.ts` to `tsconfig.node.json`'s `include`) is **necessary and sufficient** for
tests living under `src/`, and nothing more is needed.

### F5 — seen-state is ONE localStorage key, not one key per finding

The plan describes seen-state as "keyed `(deckKey, finding.id)`". The implementation uses a
single key `'tellr-viewer-seen-findings'` whose JSON value is `{deckKey: [findingId, ...]}` —
per-deck arrays inside one entry [scout]. The §K9 id-stability argument is unaffected (the
finding id is still the identity), but any test or helper written against a per-finding key
would be written against something that does not exist.

### F6 — `frontend/src/types/slide.ts` and the fixture

`slide.ts` declares `verification?` and `content_hash?` and no `findings` [scout] — B3.2's
`findings?: SlideFinding[]` addition on `SlideDeck` is genuinely new.
`frontend/tests/fixtures/findings.ts` exports `mockFindings` with three entries as the plan
describes [scout]; keep the ids, ~ten assertions in `slide-viewer.spec.ts` key on them.

---

## 8. `compute_slide_hash` — the trap, with measured digests

`src/utils/slide_hash.py:44` is `' '.join(html.split())` [scout]. The docstring example at
`:69-72` is **wrong**, exactly as the plan says. Measured digests [scout, method re-probed]:

    "<DIV CLASS='slide'>  a  </DIV>"   -> 7bf2560504df8454
    "<div class='slide'>a</div>"       -> 1be77d06060fc395     # DIFFERENT

The docstring's own pair also differs:

    "<DIV>  hello  </DIV>"  normalises to  "<div> hello </div>"   -> 4745336f4a90d58d
    "<div>Hello</div>"      normalises to  "<div>hello</div>"     -> 37ca31f435e7380a

**What DOES hold:** case-insensitivity, and collapsing *runs* of whitespace **between** tokens to
a single space. **What does NOT hold:** removal of inter-token whitespace. Assert only the
former. Carry-forward finding **11** is discharged by recording this here; B2.2's test must not
assert the false equality, and `slide_hash.py` itself is not edited.

---

## 9. Corrected test-reference counts for B2.4's R1 triage

Counts of `system_prompt` / `slide_editing_instructions` references [scout]:

| File | Plan | Actual |
|---|---|---|
| `tests/unit/test_agent_factory.py` | 41 | 41 |
| `tests/unit/test_prompt_precedence_fixes.py` | 40 | 40 |
| `tests/unit/test_design_system_compiler.py` | 17 | 17 |
| `tests/unit/test_ds_generation_state_matrix.py` | 14 | 14 |
| `tests/unit/test_migration.py` | 12 | 12 |
| `tests/unit/test_agent_config_schema.py` | 8 | 8 |
| `tests/unit/config/test_models.py` | 4 | **6** |
| `tests/unit/test_unset_agent_config_is_sql_null.py` | 2 | **4** |
| `tests/unit/test_settings_db.py` | 2 | **3** |
| `tests/unit/config/test_services.py` | 3 (path unstated) | 3 |
| `tests/unit/test_config_loader.py` | 1 | **3** |
| `tests/unit/test_default_config_integration.py` | 1 | 1 |

**Fourteen further files carry references the plan does not name** [scout]:
`test_prompt_modules.py` (16), `test_prompt_performance.py` (13), `test_slide_editing_robustness.py`
(4), `test_spotlighting.py` (4), `test_design_systems_routes.py` (2), `test_ds_asset_tool.py` (2),
`test_html_to_pptx.py` (2), `test_error_recovery.py` (2), `test_prompts_defaults.py` (2),
`test_agent.py` (1), `test_slide_context_injection.py` (1), `test_feedback_service.py` (1),
`tests/conftest.py` (1).

**Ruling (controller): the twelve named files are the triage scope; the fourteen extra are a
BREAKAGE SWEEP, not a triage list.** Most of those references will be to prompt *assembly*
(which survives) rather than to the retired columns. The obligation is: after B2.4/B2.5, the
whole suite is green by cause. Any of the fourteen that breaks gets the same per-test R1
question. Carry-forward finding **15**'s inventory half is discharged by this table plus the
fourteen-file list.

---

## 10. Per-task deltas

**B1.1** — no path corrections. `is_placeholder_record` at
`src/api/services/slide_repository.py:41-61` and its "zero production callers" claim: verify by
sweep before relying on it (scout confirmed callers are test fixtures only). Constraint 3's test
must run against the whole 66-line `get_verification_map` (§5/A2).

**B1.2** — apply §3/S3 (no missing-null-check assertion), §7/F5 (localStorage shape), §7/F6.

**B1.3** — apply §6/C2 (job position), §6/C3 (**test-summary needs**), §7/F1 (`@/ui` →
`src/ui/`), §7/F2 (dependency + lockfile), §7/F3, §7/F4.

**B1.4, B1.5, B1.6** — no corrections found. `src/domain/` has no DB imports today [scout]; keep
it that way. `src/domain/slide.py`'s `scripts` field is the `str` precedent B1.5 matches.

**B1.7** — `tests/unit/conftest.py` and `tests/integration/conftest.py` both absent, as the plan
says [scout]. `tests/integration/test_slide_row_identity_and_verdicts.py` does use
`sqlite:///:memory:` [scout] — the file-backed engine fixture is genuinely needed.
**Any new `tests/integration/test_*.py` must also be named in a CI job** or
`tests/unit/test_ci_collects_integration_tests.py` fails the PR; a `conftest.py` is not
`test_*.py` and is unaffected.

**B2.1** — §2 (keep the `Send(timeout=)` docstring). `provide_token`'s `do_connect` listener is
at `src/core/database.py:305`, not `:303` [scout]. Follow `src/core/encryption.py`'s unqualified
raw SQL.

**B2.2** — §5/A4 (`ensure_deck_token_css` import path is B3.3's, not B2.2's, but the same module
correction applies wherever the plan implies `css_utils`), §8 (the hash trap).

**B2.3** — `spec_dirty*` confirmed absent (§3/S2). The partial index is Postgres-only; use §0.3's
self-skip pattern to prove it.

**B2.4** — §4 (nine + two wrong paths), §6/C1 (`test.yml:664`), §6/C4 (the seven sites + four
test constructors), §9 (counts and the fourteen-file sweep).

**B2.5** — §6/C5 (five steps; place the strip step LAST), §0.4 (Lakebase).

**B3.1** — §3/S1 (**no columns to add**; `version` already exists), §5/A1 (`deck.deck_json =
None` is not a literal), §5 anchor table.

**B3.2** — §5 anchor table, §5/A2, §7/F6.

**B3.3** — §5/A4 (import `ensure_deck_token_css` from `src/services/design_system_templates.py`;
it PREPENDS and emits a comment marker), plus ws4a's inherited `merge_css` contract from the
merge commit `a2dcae9e`: **fold pairwise**, an empty replacement returns `existing_css` early,
dedupe key is `(lower_at_keyword, discriminator)`. **Never regenerate
`tests/sample_htmls/final_html.html`.**

---

## 11. Carry-forward findings assigned to ws4b

| # | Where it is discharged |
|---|---|
| 11 | §8 of this file, and B2.2's corrected test intent |
| 12 | B2.5 — the four steps go BEFORE `_reassign_new_objects_to_shared_owner` |
| 13 | B1.7's fixture table (methods are part of the contract) |
| 15 (inventory half) | §9 of this file |
| 22 | B2.1/B2.2 — no ordering rhetoric claiming `create_all` runs after `_run_migrations` |
| 25 | B1.1 — no `test_every_criterion_maps_into_the_closed_union` loop; B1.2 covers TS drift |
| 26 | B1.5 — `scripts: str`, picked once |
| 27 | §7/F1 |
| 29 | B1.7's `session_with_garbage_blob` trap |
