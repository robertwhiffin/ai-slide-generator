# Production dependencies with pinned versions for reproducibility
# Generated from working environment: 2025-01-09

# Core web framework
fastapi==0.121.2
# starlette pin: fastapi==0.121.2 requires starlette>=0.40.0,<0.50.0; fastapi
# 0.137 / starlette 1.3 regressed APIRouter (app.include_router() stopped
# flattening sub-router routes — caught by tests/unit/test_app_wiring.py).
# fastapi==0.121.2 already blocks 1.x transitively, but pin explicitly so
# upgrading fastapi in this file doesn't accidentally open the starlette 1.3
# regression. Resolver chose 0.49.3 for fastapi==0.121.2.
starlette==0.49.3
uvicorn==0.38.0
python-multipart==0.0.20
pydantic==2.12.4
pydantic-settings==2.12.0

# Databricks integration
databricks-sdk==0.91.0
databricks-langchain==0.9.0
databricks-mcp>=0.1.0  # MCP server tool support (DatabricksMCPClient)

# LLM framework
langgraph==1.2.10
langgraph-checkpoint==4.1.1
langgraph-prebuilt==1.1.0
langgraph-sdk==0.4.2
langchain==1.3.14
langchain-core==1.5.3
langchain-community==0.4.2
langchain-text-splitters==1.1.2
litellm==1.80.11

# MLflow and observability
mlflow==3.14.0
opentelemetry-api==1.38.0
opentelemetry-sdk==1.38.0

# HTML parsing
beautifulsoup4==4.14.2
lxml==6.0.2
tinycss2==1.3.0

# JavaScript validation (for slide editing robustness)
esprima==4.0.1

# Database
sqlalchemy==2.0.36
psycopg2-binary==2.9.10
databricks-sql-connector==3.0.1  # For Lakebase connections

# Utilities
httpx==0.28.1
jinja2==3.1.2
python-dotenv==1.2.1
pyyaml==6.0.1
pandas==2.3.3

# PowerPoint export
python-pptx==0.6.23

# Google Slides export
google-api-python-client==2.190.0
google-auth-oauthlib==1.2.4
google-auth-httplib2==0.3.0

# Encryption (Fernet symmetric encryption for OAuth credentials/tokens)
cryptography==46.0.3

# Browser automation (HTML to image rendering)
playwright==1.48.0

# Image processing
Pillow==11.3.0
svgpathtools>=1.6.0

# Note: Development dependencies are in pyproject.toml [project.optional-dependencies.dev]
# Install with: uv sync --all-extras
